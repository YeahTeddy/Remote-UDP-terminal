# 基于UDP的远程终端项目计划
## 一、项目概述
本项目基于UDP协议实现可靠的远程终端，支持多客户端并发连接、命令执行、心跳保活等功能，在应用层通过停等ARQ机制解决UDP不可靠的问题。

## 二、自定义应用层协议设计
### 报文格式（固定头部12字节 + 可变数据）
| 字段名 | 长度（字节） | 说明 |
|--------|--------------|------|
| 魔数   | 2            | 固定为0x1234，用于校验非法报文 |
| 报文类型 | 1          | 0:数据报文 1:ACK报文 2:心跳报文 3:命令中断报文 |
| 序列号 | 1            | 0-255循环，用于停等ARQ |
| 客户端ID | 4          | 唯一标识每个客户端 |
| 数据长度 | 4          | 后续数据部分的长度 |
| 数据部分 | 可变       | 最大1460字节（保证整个报文不超过1472字节，避免IP分片） |

### 协议规则
1. 所有报文必须通过魔数校验，不匹配直接丢弃
2. 停等ARQ：发送方发送报文后等待ACK，超时2秒重传，最多重传5次
3. 序列号循环使用，收到重复序列号报文直接丢弃，返回对应ACK
4. 心跳报文每5秒发送一次，服务端30秒未收到心跳视为客户端离线

## 三、模块划分
### 1. 协议处理模块
- 负责报文封包、解包
- 魔数校验、字段解析
- 报文构造工具函数

### 2. 可靠传输模块
- 停等ARQ实现：超时重传、ACK确认、去重
- 序列号管理

### 3. 服务端模块
- UDP socket绑定监听
- 客户端管理（按客户端ID区分）
- 命令执行与输出捕获
- 心跳检测与离线清理

### 4. 客户端模块
- UDP socket连接服务端
- 用户输入捕获与发送
- 服务端响应接收与显示
- 心跳定时发送
- 终端控制字符处理

## 四、实现步骤
1. 完成协议工具函数（封包、解包）
2. 实现服务端和客户端基础UDP通信
3. 集成停等ARQ可靠传输机制
4. 实现服务端命令执行功能
5. 实现多客户端区分管理
6. 实现心跳检测机制
7. 实现终端控制字符处理
8. 功能测试与bug修复

## 五、测试方案
1. 基础通信测试：客户端可连接服务端发送消息
2. 命令执行测试：ls/pwd/ps等命令可正常执行并返回结果
3. 可靠性测试：模拟网络丢包，验证重传机制正常工作
4. 多客户端测试：多个客户端同时连接，互不干扰
5. 心跳测试：客户端断开后服务端可检测到离线
6. 异常测试：非法报文、网络中断等场景程序不崩溃
