# UDP 远程终端项目实现细节说明

本文档面向刚刚学习过计算机网络的同学，按实验指导书中的任务逐项说明本项目是如何实现的。

项目核心目标是：使用 UDP 实现一个远程终端程序。客户端输入命令，服务端执行命令，再把输出结果通过 UDP 返回客户端。

由于 UDP 本身不保证可靠传输，所以项目在应用层自己设计了协议、ACK、序列号、重传、去重、心跳、多客户端管理等机制。

---

## 0. 项目整体结构

项目主要由以下文件组成：

```text
UDP/
├── common.py                         协议常量、封包/解包、通用工具函数
├── server.py                         服务端：接收命令、执行命令、返回输出、管理客户端
├── client.py                         客户端：输入命令、发送命令、接收并显示输出
├── test_all.py                       自动化测试脚本
├── README.md                         使用说明和功能总结
├── 基于UDP的远程终端实验指导书.md      实验要求
└── 实现细节说明.md                    本文档
```

可以把三个核心文件理解为：

| 文件 | 作用 |
|---|---|
| `common.py` | 双方都要遵守的“通信协议说明书” |
| `server.py` | UDP 服务端，负责执行命令 |
| `client.py` | UDP 客户端，负责用户交互 |

其中 `common.py` 非常重要，因为客户端和服务端必须使用完全相同的协议格式，否则双方就无法理解彼此发送的数据。

---

## 1. UDP 基础通信

### 1.1 任务要求

实验指导书要求：

1. 客户端可以指定服务端 IP 和端口。
2. 服务端绑定端口并接收 UDP 报文。

### 1.2 相关计算机网络知识

UDP 是一种无连接传输层协议。它和 TCP 最大的区别是：

| 对比项 | TCP | UDP |
|---|---|---|
| 是否连接 | 面向连接 | 无连接 |
| 是否可靠 | 可靠 | 不可靠 |
| 是否保证顺序 | 保证 | 不保证 |
| 是否自动重传 | 会 | 不会 |
| 通信方式 | 字节流 | 数据报 |

UDP 的核心 API 是：

```python
sendto(data, address)
recvfrom(buffer_size)
```

也就是说，发送方每次把一个完整的数据报发出去，接收方每次收到一个完整的数据报。UDP 不存在 TCP 中的“粘包”问题，但 UDP 可能丢包、乱序、重复。

### 1.3 服务端如何绑定端口

服务端在 `server.py` 中创建 UDP socket：

```python
self.sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
```

这里：

- `socket.AF_INET` 表示使用 IPv4。
- `socket.SOCK_DGRAM` 表示使用 UDP。

然后服务端绑定地址和端口：

```python
self.sock.bind((host, port))
```

默认情况下：

```python
host = '0.0.0.0'
port = 9999
```

`0.0.0.0` 的意思是监听本机所有网卡地址。也就是说，无论客户端从本机 IP、局域网 IP 还是公网 IP 访问，只要网络可达并且端口开放，服务端都可以收到 UDP 报文。

### 1.4 服务端如何接收 UDP 报文

服务端接收数据的核心代码是：

```python
data, addr = self.sock.recvfrom(1500)
```

这里：

- `data` 是客户端发来的原始字节数据。
- `addr` 是客户端地址，格式通常是 `(IP, port)`。
- `1500` 是接收缓冲区大小，和以太网 MTU 有关。

服务端收到数据后，会调用 `unpack_msg(data)` 尝试解析协议。如果解析失败，说明不是合法数据包，直接丢弃。

### 1.5 客户端如何指定服务端 IP 和端口

客户端启动时支持命令行参数：

```bash
python client.py [服务端IP] [服务端端口]
```

例如：

```bash
python client.py 127.0.0.1 9999
```

在 `client.py` 中，客户端会把服务端地址保存为：

```python
self.server_addr = (server_host, server_port)
```

后续发送命令、心跳、中断报文时，都会发到这个地址。

### 1.6 本任务实现总结

本项目已经实现 UDP 基础通信：

1. 服务端使用 UDP socket 绑定端口。
2. 客户端可以指定服务端 IP 和端口。
3. 客户端通过 `sendto()` 发送报文。
4. 服务端通过 `recvfrom()` 接收报文。
5. 服务端可以从 `addr` 中知道报文来自哪个客户端地址。

---

## 2. 自定义应用层协议封包 / 解包

### 2.1 任务要求

实验指导书要求实现自定义协议，正确解析：

1. 魔数
2. 报文类型
3. 序列号
4. 数据长度
5. 客户端 ID
6. 数据段

非法报文需要直接丢弃。

### 2.2 为什么需要自定义协议

UDP 只负责把一段字节数据从一台主机发到另一台主机。它不会告诉应用程序：

- 这段数据是命令还是输出？
- 这段数据属于哪个客户端？
- 这是不是重复数据？
- 数据长度是否合法？
- 这是不是本程序的数据包？

所以我们必须自己在 UDP 数据段内部设计一个“应用层协议头”。

可以把 UDP 报文理解为一个快递包裹：

```text
UDP 报文 = 自定义头部 + 实际数据
```

头部就像快递单，说明这个包裹是什么类型、属于谁、里面有多长数据。

### 2.3 当前协议格式

项目中协议头部定义在 `common.py`：

```python
HEADER_FORMAT = '>H B I H I'
```

它表示固定头部由以下字段组成：

| 字段 | 长度 | 含义 |
|---|---:|---|
| 魔数 | 2 字节 | 固定为 `0x5554`，用于识别合法报文 |
| 报文类型 | 1 字节 | 表示这是命令、输出、ACK、心跳还是中断 |
| 序列号 | 4 字节 | 用于 ACK、去重、保序和重传判断 |
| 数据长度 | 2 字节 | 后面数据段的长度 |
| 客户端 ID | 4 字节 | 用于区分不同客户端 |

总头部大小为：

```python
HEADER_SIZE = struct.calcsize(HEADER_FORMAT)
```

计算结果是 13 字节。

其中 `>` 表示使用大端字节序。网络协议通常使用大端字节序，也叫网络字节序。

### 2.4 魔数的作用

魔数定义为：

```python
MAGIC = 0x5554
```

魔数用于判断收到的 UDP 报文是不是本程序的报文。

UDP 端口可能收到各种无关数据。如果没有魔数，服务端可能会把无关数据当成命令解析，导致错误。

处理逻辑是：

```python
if magic != MAGIC:
    return None
```

只要魔数不匹配，就认为是非法报文，直接丢弃。

### 2.5 报文类型

当前项目定义了 5 种报文类型：

```python
TYPE_COMMAND = 0x01
TYPE_OUTPUT = 0x02
TYPE_ACK = 0x03
TYPE_HEARTBEAT = 0x04
TYPE_INTERRUPT = 0x05
```

含义如下：

| 类型 | 数值 | 方向 | 作用 |
|---|---:|---|---|
| `TYPE_COMMAND` | `0x01` | 客户端 → 服务端 | 发送用户输入的命令 |
| `TYPE_OUTPUT` | `0x02` | 服务端 → 客户端 | 返回命令输出 |
| `TYPE_ACK` | `0x03` | 双向 | 确认收到某个序列号的报文 |
| `TYPE_HEARTBEAT` | `0x04` | 客户端 → 服务端 | 心跳保活 |
| `TYPE_INTERRUPT` | `0x05` | 客户端 → 服务端 | 中断正在运行的远程命令 |

如果收到的报文类型不在合法集合中，也会直接丢弃。

### 2.6 封包过程

封包函数是：

```python
def pack_msg(msg_type: int, seq: int, client_id: int, data: bytes) -> bytes:
```

封包过程可以分为 4 步：

1. 检查报文类型是否合法。
2. 检查序列号范围是否合法。
3. 检查客户端 ID 范围是否合法。
4. 使用 `struct.pack()` 把头部字段变成字节，再拼接数据段。

核心逻辑是：

```python
header = struct.pack(HEADER_FORMAT, MAGIC, msg_type, seq, len(data), client_id)
return header + data
```

例如要发送命令 `dir`，实际发送的数据结构大概是：

```text
[魔数][类型=命令][序列号][数据长度=3][客户端ID][dir]
```

### 2.7 解包过程

解包函数是：

```python
def unpack_msg(raw_data: bytes) -> tuple[int, int, int, bytes] | None:
```

解包过程可以分为 5 步：

1. 判断数据长度是否至少包含完整头部。
2. 使用 `struct.unpack()` 解析头部字段。
3. 校验魔数。
4. 校验报文类型。
5. 校验数据长度是否和头部声明一致。

如果任何一步失败，返回 `None`。

如果成功，返回：

```python
(msg_type, seq, client_id, data)
```

服务端和客户端收到报文后，都先调用 `unpack_msg()`。如果返回 `None`，说明报文非法，直接忽略。

### 2.8 为什么要限制 UDP 报文大小

项目中定义：

```python
MAX_DATA_SIZE = 1472 - HEADER_SIZE
```

原因是以太网常见 MTU 是 1500 字节。IP 头部通常 20 字节，UDP 头部 8 字节，剩余：

```text
1500 - 20 - 8 = 1472 字节
```

如果 UDP 数据超过 1472 字节，IP 层可能会分片。分片会降低可靠性，只要一个分片丢失，整个 UDP 报文就无法恢复。

所以项目主动把单个 UDP 报文控制在 1472 字节以内。

### 2.9 本任务实现总结

本项目已经实现完整的自定义协议：

1. 使用 13 字节固定协议头。
2. 使用魔数识别合法报文。
3. 使用类型字段区分命令、输出、ACK、心跳、中断。
4. 使用 4 字节序列号支持可靠传输。
5. 使用客户端 ID 区分多客户端。
6. 使用数据长度校验报文完整性。
7. 非法报文直接丢弃，不影响程序运行。

---

## 3. 停等 ARQ 可靠传输

### 3.1 任务要求

实验指导书要求：

1. 超时重传。
2. 序列号去重。
3. ACK 确认机制。

### 3.2 为什么 UDP 需要自己做可靠传输

UDP 不保证可靠性。可能出现：

1. 客户端发送命令，但是服务端没收到。
2. 服务端收到命令并执行了，但是 ACK 丢了。
3. 客户端以为服务端没收到，于是重发命令。
4. 服务端如果不去重，可能重复执行同一条命令。

例如用户输入：

```bash
mkdir test
```

如果这条命令被重复执行，可能造成错误或副作用。因此必须通过序列号和 ACK 避免重复执行。

### 3.3 停等 ARQ 的基本思想

停等 ARQ 的流程是：

```text
发送方发送一个报文
        ↓
等待接收方回复 ACK
        ↓
如果收到 ACK：发送下一个报文
如果超时：重发当前报文
```

“停等”的意思是：发送方一次只发送一个需要确认的报文，收到 ACK 后才发送下一个。

### 3.4 客户端发送命令的可靠传输

客户端发送命令时调用：

```python
_send_reliable(data)
```

核心状态包括：

```python
self.send_seq
self.waiting_seq
self.ack_event
```

含义如下：

| 变量 | 含义 |
|---|---|
| `send_seq` | 当前要发送的命令序列号 |
| `waiting_seq` | 当前正在等待 ACK 的序列号 |
| `ack_event` | 线程同步对象，用于通知 ACK 已收到 |

发送流程：

1. 使用当前 `send_seq` 封装命令报文。
2. 调用 `sendto()` 发送给服务端。
3. 等待 `ack_event`，超时时间为 2 秒。
4. 如果 2 秒内收到 ACK，说明服务端收到命令。
5. 客户端把 `send_seq` 加 1。
6. 如果没有收到 ACK，最多重传 5 次。

简化后的逻辑如下：

```python
while retry_count < max_retries:
    send(command_packet)
    if wait_ack(timeout=2):
        send_seq += 1
        return True
    retry_count += 1
return False
```

### 3.5 服务端如何回复 ACK

服务端收到命令后，先检查序列号是否是当前期望的序列号。

如果是期望序列号，说明这是新命令：

```python
client.recv_expected_seq = next_data_seq(client.recv_expected_seq)
ack_msg = pack_msg(TYPE_ACK, seq, client_id, b'')
self.sock.sendto(ack_msg, addr)
```

也就是说，服务端会：

1. 推进期望序列号。
2. 回复 ACK。
3. 执行命令。

### 3.6 服务端如何去重

如果收到的命令序列号不是期望值，服务端不会执行命令，只回复 ACK：

```python
if seq != client.recv_expected_seq:
    ack_msg = pack_msg(TYPE_ACK, seq, client_id, b'')
    self.sock.sendto(ack_msg, addr)
    return
```

这样可以解决 ACK 丢失导致的重复命令问题。

例如：

1. 客户端发送序列号 0 的命令。
2. 服务端收到并执行，回复 ACK。
3. ACK 在网络中丢失。
4. 客户端超时，重发序列号 0 的命令。
5. 服务端发现自己已经期待序列号 1，所以序列号 0 是重复包。
6. 服务端只回复 ACK，不重复执行命令。

### 3.7 输出方向的可靠传输

基础要求是停等 ARQ，但当前项目在服务端输出方向做了进一步增强：使用小窗口可靠传输。

原因是命令输出可能很大，例如：

```bash
dir
ipconfig
cat large_file.txt
```

如果输出很大，一次只能发一个分片并等待 ACK，效率会比较低。因此服务端输出方向使用了窗口大小：

```python
OUTPUT_WINDOW_SIZE = 8
```

也就是说，服务端最多可以连续发送 8 个输出分片，然后等待 ACK 推动窗口向前移动。

### 3.8 本任务实现总结

本项目在可靠传输方面实现了：

1. 命令方向使用停等 ARQ。
2. 每个命令报文都有序列号。
3. 服务端收到命令后回复 ACK。
4. 客户端超时会重传。
5. 服务端使用序列号避免重复执行命令。
6. 输出方向使用窗口机制提高大输出传输效率。

---

## 4. 基础终端命令执行

### 4.1 任务要求

实验指导书要求：

1. 支持 `ls`、`pwd`、`ps`、`ip addr` 等非交互式命令。
2. 正确显示输出。
3. 处理 `\n`、`\r`、`\b` 等基础控制字符。

### 4.2 远程终端的基本流程

远程终端可以理解为：

```text
用户在客户端输入命令
        ↓
客户端把命令通过 UDP 发给服务端
        ↓
服务端启动子进程执行命令
        ↓
服务端读取命令 stdout/stderr
        ↓
服务端把输出通过 UDP 发回客户端
        ↓
客户端显示输出
```

### 4.3 服务端如何执行命令

服务端收到命令后，会启动一个线程执行：

```python
threading.Thread(
    target=self._execute_and_respond,
    args=(client_id, cmd),
    daemon=True
).start()
```

这样做的好处是：

1. 服务端主接收循环不会被某条命令阻塞。
2. 多个客户端可以同时执行命令。
3. 某个命令执行较慢时，不影响其他客户端。

### 4.4 Windows 和 Linux/macOS 的命令执行差异

服务端根据操作系统选择不同的 shell。

Windows 下：

```python
subprocess.Popen(
    ['cmd.exe', '/c', cmd],
    stdout=subprocess.PIPE,
    stderr=subprocess.PIPE,
    cwd=client.cwd,
    creationflags=subprocess.CREATE_NEW_PROCESS_GROUP
)
```

Linux/macOS 下：

```python
subprocess.Popen(
    ['bash', '-c', cmd],
    stdout=subprocess.PIPE,
    stderr=subprocess.PIPE,
    cwd=client.cwd,
    preexec_fn=os.setsid
)
```

其中：

| 参数 | 作用 |
|---|---|
| `stdout=subprocess.PIPE` | 捕获标准输出 |
| `stderr=subprocess.PIPE` | 捕获错误输出 |
| `cwd=client.cwd` | 指定命令运行目录 |
| `cmd.exe /c` | Windows 下执行一条命令后退出 |
| `bash -c` | Linux/macOS 下执行一条命令后退出 |

### 4.5 stdout 和 stderr 是什么

命令执行时通常有两个输出通道：

| 通道 | 含义 |
|---|---|
| stdout | 正常输出 |
| stderr | 错误输出 |

例如：

```bash
dir
```

正常列出目录内容，输出走 stdout。

如果输入不存在的命令：

```bash
abc_not_exist
```

错误提示通常走 stderr。

项目会同时读取 stdout 和 stderr，然后都发送给客户端显示。

### 4.6 实时输出实现

早期实现可以等命令完全结束后再一次性返回输出，但这样不适合 `ping` 这类持续输出命令。

当前项目使用 `_stream_pipe()` 持续读取子进程输出：

```python
chunk = read_chunk(4096)
```

读取到一段输出后，立即调用：

```python
self._send_output_reliable(client_id, text.encode('utf-8'))
```

这样客户端不必等命令完全结束，就可以看到已经产生的输出。

需要注意的是，本项目没有使用 PTY，所以有些程序如果发现自己不是运行在真实终端中，可能会自行缓冲输出。对于已经 flush 出来的内容，项目可以实时转发。

### 4.7 命令执行超时

为了避免服务端被一个永远不结束的命令占用，项目设置了 30 秒超时：

```python
proc.wait(timeout=30)
```

如果超时：

1. 服务端杀掉进程树。
2. 返回错误提示：`Error: Command execution timed out`。
3. 清理当前进程状态。

这可以防止服务端资源被长期占用。

### 4.8 cd 命令的特殊处理

普通情况下，`cd` 是 shell 内置命令。如果直接用子进程执行：

```bash
cd ..
```

只会改变子进程自己的目录，子进程退出后目录变化就消失了。

为了让远程终端更像真实终端，项目把 `cd` 做成服务端内置逻辑。

每个客户端都有自己的当前目录：

```python
client.cwd
```

当服务端识别到命令是 `cd` 时，不启动普通子进程，而是修改该客户端的 `client.cwd`。

这样：

```bash
cd ..
pwd
```

后面的 `pwd` 或 Windows 下的 `echo %CD%` 会显示改变后的目录。

不同客户端的 `cwd` 是独立的，因此一个客户端 `cd ..` 不会影响另一个客户端。

### 4.9 控制字符处理

网络传输的命令本质上是字节。用户输入中可能包含：

| 控制字符 | 含义 |
|---|---|
| `\n` | 换行 |
| `\r` | 回车 |
| `\b` | 退格 |
| `\x7f` | Delete |

项目在 `normalize_command_input()` 中处理这些字符：

1. 把 `\r\n` 和 `\r` 统一成 `\n`。
2. 遇到退格或 Delete，就删除前一个字符。
3. 过滤 ANSI 转义序列。

例如：

```text
AB\bC
```

处理后变成：

```text
AC
```

### 4.10 本任务实现总结

本项目在命令执行方面实现了：

1. 服务端可以执行非交互式命令。
2. Windows 使用 `cmd.exe /c`。
3. Linux/macOS 使用 `bash -c`。
4. 支持 stdout 和 stderr 返回。
5. 支持实时读取已经产生的输出。
6. 支持命令超时清理。
7. 支持每个客户端独立的持久 `cd`。
8. 支持基础控制字符处理。

---

## 5. 多客户端支持

### 5.1 任务要求

实验指导书要求：

1. 服务端根据客户端 ID 区分不同用户。
2. 多客户端同时连接不混乱、不互相干扰。

### 5.2 为什么不能只用 IP 和端口区分客户端

UDP 是无连接的。服务端每次收到报文时，可以看到报文来源地址 `addr`，但只依赖 `addr` 有一些问题：

1. 客户端可能重启后端口变化。
2. NAT 环境下多个客户端可能共享公网 IP。
3. 实验要求明确使用客户端 ID。

所以项目为每个客户端生成一个随机 ID：

```python
self.client_id = random.randint(1, 2**32 - 1)
```

客户端发出的每个报文都会携带这个 ID。

### 5.3 服务端如何保存客户端状态

服务端定义了 `ClientInfo` 类，每个客户端对应一个对象。

每个客户端保存：

| 字段 | 含义 |
|---|---|
| `addr` | 客户端当前 UDP 地址 |
| `last_heartbeat` | 最后一次心跳时间 |
| `recv_expected_seq` | 服务端期待收到的下一个命令序列号 |
| `send_seq` | 服务端发送输出时使用的序列号 |
| `cwd` | 该客户端独立的当前工作目录 |
| `current_process` | 该客户端当前正在执行的命令进程 |
| `process_lock` | 保护进程状态的锁 |
| `send_lock` | 保护输出发送状态的锁 |

服务端用字典保存所有客户端：

```python
self.clients = {}
```

字典结构可以理解为：

```text
client_id → ClientInfo
```

### 5.4 新客户端注册流程

服务端收到报文后，先解析出 `client_id`。

如果这个 ID 之前没有出现过：

```python
self.clients[client_id] = ClientInfo(addr)
```

这表示注册一个新客户端。

之后每次收到这个客户端的报文，服务端都会更新：

```python
client.addr = addr
client.last_heartbeat = time.time()
```

这样即使客户端的 UDP 端口变化，服务端也能使用最新地址回复。

### 5.5 多客户端为什么不会混乱

多客户端不混乱的关键是：

1. 每个报文都有客户端 ID。
2. 服务端每个客户端都有独立状态。
3. 每个客户端有独立的命令接收序列号。
4. 每个客户端有独立的输出发送序列号。
5. 每个客户端有独立的当前目录。
6. 每个客户端有独立的当前进程。

因此，客户端 A 的输出不会发给客户端 B，客户端 A 的 `cd` 也不会影响客户端 B。

### 5.6 本任务实现总结

本项目已经实现多客户端支持：

1. 客户端随机生成 32 位客户端 ID。
2. 所有报文都携带客户端 ID。
3. 服务端用 `clients` 字典按 ID 管理客户端。
4. 每个客户端有独立的序列号、目录、进程和心跳时间。
5. 多客户端同时执行命令不会互相干扰。

---

## 6. 心跳检测

### 6.1 任务要求

实验指导书要求：

1. 客户端每 5 秒发送心跳。
2. 服务端收到心跳后回复 ACK。
3. 长时间无心跳视为离线。

### 6.2 为什么需要心跳

UDP 是无连接协议。TCP 连接断开时，操作系统通常能检测到连接关闭；但 UDP 没有连接状态。

也就是说，如果客户端直接关闭，服务端不会自动知道它已经离线。

心跳机制就是让客户端定期告诉服务端：

```text
我还在线
```

如果服务端很久没有收到某个客户端的心跳，就认为它已经离线。

### 6.3 心跳报文

心跳报文使用类型：

```python
TYPE_HEARTBEAT = 0x04
```

心跳序列号使用一个特殊值：

```python
HEARTBEAT_SEQ = 0xFFFFFFFF
```

这样普通命令序列号和心跳序列号不会混淆。

### 6.4 客户端心跳发送

客户端启动后会先调用 `_connect_to_server()`，通过心跳检查服务端是否在线。

如果服务端 2 秒内没有回复，客户端认为连接失败。

连接成功后，客户端启动心跳线程：

```python
heartbeat_thread = threading.Thread(target=self._heartbeat_loop, daemon=True)
```

心跳线程每 5 秒运行一次：

1. 发送心跳。
2. 等待服务端 ACK。
3. 如果 2 秒内没有收到 ACK，提示连接异常并退出。

### 6.5 服务端心跳处理

服务端收到心跳后：

1. 更新该客户端的 `last_heartbeat`。
2. 打印心跳日志。
3. 回复 ACK。

服务端回复的心跳 ACK 里还携带了提示符信息：

```python
payload = pack_prompt_info(self.server_user, self.server_host, client.cwd)
```

包括：

1. 服务端用户名
2. 服务端主机名
3. 当前客户端所在目录

客户端收到后可以显示类似：

```text
[user@host dir]$
```

这种 SSH 风格提示符。

### 6.6 服务端离线清理

服务端有清理线程 `_cleanup_loop()`。

它会定期检查：

```python
if now - client.last_heartbeat > 30:
```

如果某个客户端超过 30 秒没有心跳：

1. 如果该客户端有正在执行的进程，杀掉进程树。
2. 从 `self.clients` 中删除该客户端。
3. 打印客户端超时移除日志。

### 6.7 本任务实现总结

本项目已经实现心跳机制：

1. 客户端启动时先通过心跳确认服务端可达。
2. 客户端每 5 秒发送心跳。
3. 服务端收到心跳后回复 ACK。
4. 客户端心跳 ACK 超时会提示连接异常。
5. 服务端 30 秒无心跳会清理离线客户端。
6. 心跳 ACK 还用于同步提示符信息。

---

## 7. 基本异常处理

### 7.1 任务要求

实验指导书要求处理：

1. 命令不存在。
2. 服务端不可达。
3. 网络中断提示。
4. 程序不崩溃。

### 7.2 非法报文处理

非法报文可能包括：

1. 魔数不对。
2. 类型不合法。
3. 长度不足。
4. 数据长度和头部声明不一致。

项目在 `unpack_msg()` 中统一处理这些情况。如果非法，返回 `None`。

接收方逻辑是：

```python
msg = unpack_msg(data)
if not msg:
    continue
```

这表示非法报文直接跳过，不影响程序继续运行。

### 7.3 命令不存在处理

命令不存在时，shell 会把错误信息写入 stderr。

项目启动子进程时捕获 stderr：

```python
stderr=subprocess.PIPE
```

然后服务端读取 stderr 并发送给客户端。因此用户可以看到错误提示，而不是服务端崩溃。

### 7.4 服务端不可达处理

客户端启动时会先发送心跳并等待 ACK。

如果服务端不可达，客户端会输出类似：

```text
Connection failed: server 127.0.0.1:9999 did not respond
```

命令发送时，如果多次重传仍然没有 ACK，会输出：

```text
Send failed after retries: server may be unreachable or network interrupted
```

### 7.5 网络中断处理

客户端心跳线程会持续检查服务端是否还在线。

如果已经连接成功，但后续心跳没有收到 ACK，客户端会提示：

```text
Connection error: server did not respond to heartbeat, exiting
```

这样用户可以知道不是命令没有输出，而是连接出问题了。

### 7.6 进程异常和超时处理

服务端执行命令时使用 `try/except/finally`：

1. 如果启动命令失败，返回错误信息。
2. 如果命令超过 30 秒，杀掉进程。
3. 无论成功失败，最后都会清理 `current_process`。
4. 最后发送输出结束标记，让客户端恢复提示符。

### 7.7 本任务实现总结

本项目的异常处理包括：

1. 非法协议报文直接丢弃。
2. 命令不存在时返回错误输出。
3. 服务端不可达时客户端能检测并提示。
4. 网络中断时客户端通过心跳检测并退出。
5. 命令超时时服务端会杀掉进程树。
6. 接收循环和执行逻辑都有异常捕获，避免程序崩溃。

---

## 8. 终端控制字符增强

### 8.1 任务要求

进阶功能要求：

1. 支持 Ctrl+C 中断服务端正在运行的命令。
2. 正确处理退格、换行、Tab。
3. 过滤 ANSI 转义序列，避免乱码。

### 8.2 Ctrl+C 为什么需要特殊处理

在本地终端中，按 Ctrl+C 通常会给当前进程发送中断信号。

但远程终端中，真正执行命令的是服务端子进程，而不是客户端本地进程。

所以客户端按 Ctrl+C 时，不能只中断客户端自己，而应该通知服务端：

```text
请中断这个客户端当前正在执行的命令
```

### 8.3 中断报文设计

项目定义了中断报文：

```python
TYPE_INTERRUPT = 0x05
```

客户端检测到命令执行期间 Ctrl+C 后，调用：

```python
_send_interrupt()
```

它会向服务端发送一个 `TYPE_INTERRUPT` 报文。

### 8.4 服务端如何中断进程

服务端收到中断报文后，找到该客户端当前进程：

```python
proc = client.current_process
```

然后根据操作系统执行不同中断方式。

Windows 下：

1. 尝试发送 `CTRL_BREAK_EVENT`。
2. 如果进程还没退出，则使用 `taskkill /F /T /PID` 杀掉进程树。

Linux/macOS 下：

1. 给进程组发送 `SIGINT`。
2. 如果失败，杀掉进程组。

这可以让 `ping` 等长时间运行的命令被远程中断。

### 8.5 退格、换行和 Tab

`normalize_command_input()` 负责处理控制字符。

当前实现：

1. `\r\n` 和 `\r` 统一成 `\n`。
2. `\b` 和 `\x7f` 表示删除前一个字符。
3. Tab 会作为字符保留并传给远端 shell。

例如：

```text
echo AB\bC
```

处理后执行的是：

```text
echo AC
```

### 8.6 ANSI 转义序列过滤

ANSI 转义序列常用于控制终端颜色、光标位置等，例如：

```text
\x1b[31m红色文字\x1b[0m
```

如果直接显示这些控制字符，可能出现乱码或奇怪的光标移动。

项目使用正则表达式过滤 ANSI 转义序列：

```python
ANSI_ESCAPE_RE = re.compile(...)
```

并通过：

```python
strip_ansi_sequences(data)
```

过滤输入和输出中的 ANSI 控制序列。

### 8.7 本任务实现总结

本项目已经实现终端控制字符增强：

1. 支持 Ctrl+C 远程中断服务端命令。
2. 支持退格和 Delete。
3. 支持回车和换行规范化。
4. Tab 会透传给远端 shell。
5. 输入和输出中的 ANSI 转义序列会被过滤。

需要注意：Tab 不是完整 SSH 式远程补全，因为项目没有使用 PTY。

---

## 9. 输出方向滑动窗口可靠传输

### 9.1 任务要求

进阶功能要求：

1. 回退 N 步 ARQ。
2. 滑动窗口发送。
3. 提高大批量输出传输效率。

### 9.2 停等 ARQ 的效率问题

停等 ARQ 一次只能发送一个分片，必须等待 ACK 后才能发送下一个。

如果网络延迟较大，或者输出内容很大，效率会比较低。

例如输出有 100 个分片，停等 ARQ 流程是：

```text
发第1个 → 等ACK → 发第2个 → 等ACK → ... → 发第100个
```

滑动窗口可以改成：

```text
一次最多发8个 → 哪些收到ACK就向前滑动窗口 → 继续发送后面的
```

### 9.3 当前窗口大小

项目定义：

```python
OUTPUT_WINDOW_SIZE = 8
```

表示服务端向客户端发送输出时，最多允许 8 个未确认输出分片同时在网络中传输。

### 9.4 输出分片

如果命令输出超过 `MAX_DATA_SIZE`，服务端会把输出切成多个分片：

```python
chunks = [data[i:i + MAX_DATA_SIZE] for i in range(0, len(data), MAX_DATA_SIZE)]
```

每个分片都有自己的序列号。

### 9.5 服务端窗口发送逻辑

服务端 `_send_output_chunks_reliable()` 中维护两个重要变量：

| 变量 | 含义 |
|---|---|
| `base` | 当前窗口中最早未确认的分片位置 |
| `next_to_send` | 下一个准备发送的分片位置 |

发送逻辑如下：

1. 当窗口没满时，继续发送新分片。
2. 每个分片发送后记录发送时间。
3. 收到 ACK 后，把对应分片标记为已确认。
4. 如果窗口最前面的分片已确认，`base` 向前移动。
5. 如果某个未确认分片超时，重传。
6. 超过最大重传次数则发送失败。

### 9.6 客户端乱序缓冲

UDP 可能乱序。比如服务端发送分片 0、1、2，客户端可能先收到 2，再收到 0。

客户端使用：

```python
self.recv_expected_seq
self.recv_buffer
```

来处理乱序。

如果收到的是期望序列号：

1. 立即显示。
2. 期望序列号加 1。
3. 检查缓冲区里是否已经有后续分片。
4. 如果有，按顺序继续显示。

如果收到的是未来序列号：

1. 先放入 `recv_buffer`。
2. 暂时不显示，避免输出乱序。

### 9.7 每个输出包都回复 ACK

客户端收到输出包后，不管它是否是当前期望序列号，都会回复 ACK：

```python
ack_msg = pack_msg(TYPE_ACK, seq, self.client_id, b'')
self.sock.sendto(ack_msg, self.server_addr)
```

这样服务端能及时知道哪些分片已经到达。

### 9.8 本任务实现总结

本项目实现了输出方向滑动窗口可靠传输：

1. 服务端输出按 `MAX_DATA_SIZE` 分片。
2. 服务端最多同时发送 8 个未确认分片。
3. 客户端每收到一个输出分片就回复 ACK。
4. 服务端根据 ACK 推动窗口。
5. 超时未确认的分片会重传。
6. 客户端支持乱序缓冲和按序显示。

说明：当前滑动窗口主要用于服务端输出方向，客户端命令输入方向仍使用停等 ARQ。

---

## 10. 实时命令输出

### 10.1 任务要求

进阶功能要求：

1. 支持 `ping` 这类持续输出命令。
2. 实时回显，不等待命令结束。

### 10.2 为什么普通方式不够

如果使用：

```python
stdout, stderr = proc.communicate()
```

程序会等命令完全结束后，才一次性拿到所有输出。

对于短命令没问题，例如：

```bash
echo hello
```

但是对于长命令，例如：

```bash
ping 127.0.0.1
```

用户希望看到它一边运行一边输出，而不是等命令结束后才看到全部内容。

### 10.3 当前实现方式

服务端启动子进程后，会为 stdout 和 stderr 各启动一个读取线程：

```python
for pipe in (proc.stdout, proc.stderr):
    thread = threading.Thread(target=self._stream_pipe, args=(client_id, pipe), daemon=True)
    thread.start()
```

每个读取线程都会不断从管道中读取数据：

```python
chunk = read_chunk(4096)
```

只要读到输出，就立即通过 UDP 可靠发送给客户端：

```python
self._send_output_reliable(client_id, text.encode('utf-8'))
```

因此，命令一边运行，服务端一边发送输出，客户端也能一边显示输出。

### 10.4 输出结束标记

客户端需要知道一条命令什么时候结束，否则它不知道什么时候显示下一条提示符。

所以服务端在命令执行结束后，会额外发送一个结束标记：

```python
pack_output_done(client.cwd)
```

这个结束标记中还包含当前工作目录。客户端收到后：

1. 知道本次命令输出已经结束。
2. 更新提示符中的目录。
3. 恢复等待下一条命令输入。

### 10.5 本任务实现总结

本项目已经实现实时命令输出：

1. 服务端使用子线程持续读取 stdout 和 stderr。
2. 读到输出后立即发送，不等待命令完全结束。
3. 客户端收到输出后立即显示。
4. 命令结束后服务端发送结束标记。
5. 对 `ping` 或主动 flush 输出的命令，可以做到实时回显。

限制是：由于没有使用 PTY，某些程序可能自己缓冲输出，这种情况下程序本身不 flush，服务端就读不到，自然也无法立即转发。

---

## 11. 基本测试与验证

### 11.1 自动化测试脚本

项目提供了 `test_all.py`，用于验证主要功能。

运行方式：

```bash
python test_all.py
```

测试内容包括：

1. 协议封包和解包。
2. 魔数校验。
3. 非法类型拒绝。
4. 长度错误拒绝。
5. 超长数据拒绝。
6. ANSI 转义序列过滤。
7. 心跳 ACK。
8. 客户端启动时检测服务端是否可达。
9. 命令执行。
10. 中文或 UTF-8 输出解码。
11. 实时输出。
12. 大输出窗口传输。
13. 多客户端隔离。
14. 心跳超时清理。
15. 停等 ARQ 去重。
16. 退格、回车、Tab 处理。
17. 命令不存在时返回错误。
18. 非法 UDP 报文不导致服务端崩溃。
19. `cd` 命令持久化。

### 11.2 为什么要做自动化测试

网络程序很容易出现隐藏问题，例如：

1. ACK 丢失导致重复执行。
2. 大输出分片乱序。
3. 多客户端输出串扰。
4. 非法包导致服务端崩溃。
5. 心跳超时没有清理。

手动测试容易漏掉这些情况。自动化测试可以快速验证核心功能是否仍然正常。

### 11.3 手动测试建议

除了自动化测试，还可以手动测试：

1. 启动服务端：

```bash
python server.py 9999
```

2. 启动客户端：

```bash
python client.py 127.0.0.1 9999
```

3. 输入普通命令：

Windows：

```bash
dir
ipconfig
echo hello
```

Linux/macOS：

```bash
ls
pwd
ps
ip addr
echo hello
```

4. 测试目录切换：

```bash
cd ..
```

然后 Windows 下执行：

```bash
echo %CD%
```

Linux/macOS 下执行：

```bash
pwd
```

5. 测试长输出：

```bash
python -c "print('A' * 20000)"
```

6. 测试实时输出：

```bash
ping 127.0.0.1
```

7. 命令运行时按 Ctrl+C，观察是否中断远程命令。

---

## 12. 当前没有实现的拔高功能

实验指导书中还有 3 个拔高功能，当前项目没有实现。

### 12.1 动态滑动窗口流量控制

当前项目有固定窗口：

```python
OUTPUT_WINDOW_SIZE = 8
```

但它不会根据客户端接收缓冲区大小动态调整窗口。

也就是说，当前实现的是“固定大小滑动窗口可靠传输”，不是完整的“根据接收端缓冲区调整发送速度”。

### 12.2 终端窗口大小同步

当前客户端没有把终端行数、列数发送给服务端。

服务端也没有使用 `WINCH` 或类似机制设置远端伪终端大小。

因此不支持真正的终端窗口大小同步。

### 12.3 top / vim 类全屏交互式程序

`top`、`vim` 这类程序需要完整伪终端 PTY 支持。

当前项目使用的是：

```python
stdout=subprocess.PIPE
stderr=subprocess.PIPE
```

这适合非交互式命令，但不等价于真实终端。

因此当前不支持完整的：

1. 光标移动。
2. 全屏刷新。
3. 实时交互输入。
4. `vim` 编辑。
5. `top` 动态界面。

---

## 13. 从网络角度理解整个项目

可以用下面这张流程图理解完整通信过程：

```text
客户端输入命令
    ↓
pack_msg() 封装为 TYPE_COMMAND 报文
    ↓
UDP sendto() 发送给服务端
    ↓
服务端 recvfrom() 收到报文
    ↓
unpack_msg() 校验魔数、类型、长度
    ↓
检查序列号，回复 ACK
    ↓
服务端执行命令
    ↓
读取 stdout/stderr
    ↓
把输出切成多个 TYPE_OUTPUT 分片
    ↓
通过滑动窗口发送给客户端
    ↓
客户端收到每个输出分片后回复 ACK
    ↓
客户端按序显示输出
    ↓
服务端发送输出结束标记
    ↓
客户端显示下一条提示符
```

从计算机网络课程知识来看，本项目实际是在 UDP 上实现了类似 TCP 的一部分能力：

| TCP 提供的能力 | 本项目如何在 UDP 上实现 |
|---|---|
| 连接识别 | 客户端 ID + 心跳 |
| 可靠传输 | ACK + 超时重传 |
| 有序交付 | 序列号 + 接收缓冲 |
| 去重 | 期望序列号判断 |
| 流量改善 | 输出方向滑动窗口 |
| 连接保活 | 心跳检测 |
| 异常检测 | 超时和重传次数限制 |

但本项目并没有完整实现 TCP，例如没有拥塞控制、动态流量控制、三次握手、四次挥手等。

---

## 14. 总结

本项目围绕“用 UDP 实现远程终端”完成了以下核心工作：

1. 使用 UDP socket 完成基础通信。
2. 设计 13 字节应用层协议头。
3. 使用魔数和类型字段识别合法报文。
4. 使用客户端 ID 支持多客户端。
5. 使用序列号、ACK、超时重传实现可靠命令传输。
6. 使用序列号去重，避免重复执行命令。
7. 服务端调用系统 shell 执行非交互式命令。
8. 捕获 stdout/stderr 并返回客户端。
9. 支持实时输出。
10. 支持输出方向滑动窗口传输。
11. 支持心跳保活和离线清理。
12. 支持 Ctrl+C 中断远程命令。
13. 支持退格、回车、换行、Tab 和 ANSI 过滤。
14. 支持非法报文、命令错误、网络不可达等异常处理。

如果只看实验指导书的基础功能，本项目已经全部完成；如果看进阶功能，也已经实现了主要内容。拔高部分因为需要 PTY、终端窗口同步和动态流量控制，目前没有实现。