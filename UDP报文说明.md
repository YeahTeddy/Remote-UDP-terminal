# UDP 报文说明

本文档单独说明本项目中所有 UDP 报文的格式、字段含义、报文类型以及 Wireshark 抓包时的识别方法。

项目协议定义文件：`common.py`

---

## 1. 项目 UDP 通信总体说明

本项目基于 UDP 实现远程终端。

UDP 本身只负责把一段字节数据从客户端发送到服务端，或者从服务端发送回客户端。UDP 不保证可靠，也不会自动告诉程序这个包是命令、输出、心跳还是 ACK。

所以本项目在 UDP 数据部分内部设计了一层自定义应用层协议。

每个 UDP 包的结构可以理解为：

```text
UDP 头部 + 项目自定义协议头 + 项目数据段
```

在 Wireshark 中，UDP 头部由系统显示；我们主要关心 UDP Payload，也就是项目自定义协议部分。

---

## 2. UDP Payload 总体格式

本项目每个 UDP Payload 都由两部分组成：

```text
13 字节固定协议头 + 可变长度数据段
```

协议头格式为：

```python
HEADER_FORMAT = '>H B I H I'
```

其中 `>` 表示大端字节序，也就是网络字节序。

字段如下：

| 字段      |     长度 | 含义                                      |
| --------- | -------: | ----------------------------------------- |
| 魔数      |   2 字节 | 固定为 `0x5554`，用于识别本项目合法报文 |
| 报文类型  |   1 字节 | 区分命令、输出、ACK、心跳等报文           |
| 序列号    |   4 字节 | 用于 ACK、重传、去重、排序                |
| 数据长度  |   2 字节 | 表示后面数据段 payload 的长度             |
| 客户端 ID |   4 字节 | 用于区分不同客户端                        |
| 数据段    | 可变长度 | 命令内容、输出内容、控制信息等            |

协议头总长度：

```text
2 + 1 + 4 + 2 + 4 = 13 字节
```

---

## 3. Wireshark 中如何拆解一个 UDP 包

假设 Wireshark 里看到 UDP Payload 十六进制如下：

```text
55 54 01 00 00 00 00 00 04 00 00 00 01 70 77 64 0a
```

可以这样拆：

```text
55 54          魔数 0x5554
01             报文类型 TYPE_COMMAND
00 00 00 00    序列号 seq=0
00 04          数据长度 4 字节
00 00 00 01    client_id=1
70 77 64 0a    数据段 payload
```

其中：

```text
70 77 64 0a
```

转成文本是：

```text
pwd\n
```

所以这个 UDP 包表示：

```text
客户端发送了一条普通命令 pwd
```

---

## 4. 序列号字段详细说明

项目自定义协议头中的第 4 到第 7 个字节是序列号字段，也就是：

```text
seq，长度 4 字节
```

在十六进制报文中，它位于报文类型后面。例如：

```text
55 54 07 aa aa aa aa 00 08 55 9e 99 dc 00 53 5a 3a 00 12 00 de
```

可以拆成：

```text
55 54          magic
07             type = TYPE_RESIZE
aa aa aa aa    seq
00 08          data_len
55 9e 99 dc    client_id
00 53 5a 3a... payload
```

所以这里的：

```text
aa aa aa aa
```

不是窗口大小，也不是客户端 ID，而是这个 `TYPE_RESIZE` 报文的序列号。

---

### 4.1 序列号的字节序和取值范围

序列号占 4 字节，使用大端序，也就是网络字节序。

取值范围是：

```text
0x00000000 到 0xffffffff
```

其中：

```text
0xffffffff
```

被项目保留给心跳报文使用。

普通数据序列号使用：

```text
0x00000000 到 0xfffffffe
```

当序列号到达普通数据上限后，会回绕到 0。

---

### 4.2 不同报文的序列号来源

为了避免普通命令、PTY 输入和窗口大小同步互相干扰，客户端给不同用途的报文设置了不同的序列号区间。

| 报文类型 | 方向 | 序列号来源 | 初始值或特点 |
|---|---|---|---|
| `TYPE_COMMAND` | 客户端 → 服务端 | `send_seq` | 从 `0` 开始，收到 ACK 后递增 |
| `TYPE_OUTPUT` | 服务端 → 客户端 | 服务端每个客户端自己的 `send_seq` | 从 `0` 开始，每个输出分片递增 |
| `TYPE_ACK` | 双向 | 被确认报文的 seq | ACK 的 seq 等于它确认的报文 seq |
| `TYPE_HEARTBEAT` | 客户端 → 服务端 | `HEARTBEAT_SEQ` | 固定为 `ff ff ff ff` |
| `TYPE_INTERRUPT` | 客户端 → 服务端 | 当前命令发送序列号 | 用于标识一次中断请求 |
| `TYPE_STDIN` | 客户端 → 服务端 | `stdin_seq` | 初始约为序列号空间的三分之一 |
| `TYPE_RESIZE` | 客户端 → 服务端 | `resize_seq` | 初始约为序列号空间的三分之二 |
| `TYPE_WINDOW_UPDATE` | 客户端 → 服务端 | 可使用当前控制序列号 | 重点信息在 `\0WIN:` payload 中 |

其中 `TYPE_STDIN` 和 `TYPE_RESIZE` 使用不同初始区间，是为了让抓包时更容易区分它们与普通命令序列号，也避免不同控制通道的 ACK 判断互相影响。

因此你在抓包中看到：

```text
aa aa aa aa
```

通常说明这是某个独立序列号区间里的当前值。它本身没有特殊业务含义，只表示：

```text
这个报文的编号
```

---

### 4.3 为什么 Resize 报文会出现 `aa aa aa aa`

客户端中 Resize 序列号初始值接近普通数据序列号空间的三分之二。

普通数据序列号空间为：

```text
0x00000000 到 0xfffffffe
```

三分之二附近的十六进制值就是：

```text
0xaaaaaaaa
```

所以第一次或早期的窗口大小同步报文中，经常可以看到：

```text
aa aa aa aa
```

它表示：

```text
TYPE_RESIZE 报文的 seq = 0xaaaaaaaa
```

也就是十进制：

```text
2863311530
```

这个值不是随机乱码，而是客户端为 resize 通道安排的初始序列号。

---

### 4.4 ACK 中的序列号如何对应原报文

ACK 报文的类型是：

```text
TYPE_ACK = 0x03
```

ACK 的 seq 字段表示它确认的是哪个序列号。

例如客户端发送 Resize：

```text
55 54 07 aa aa aa aa 00 08 ...
```

服务端如果收到并处理成功，会回复：

```text
55 54 03 aa aa aa aa 00 00 ...
```

其中：

```text
03 = TYPE_ACK
aa aa aa aa = 确认 seq=0xaaaaaaaa 的 Resize 报文
```

所以在 Wireshark 中确认某个 Resize 是否被服务端收到，可以看是否存在反方向 ACK：

```text
服务端 → 客户端
55 54 03 aa aa aa aa
```

同理，普通命令也是这样：

```text
客户端 → 服务端：55 54 01 00 00 00 00 ...
服务端 → 客户端：55 54 03 00 00 00 00 ...
```

表示服务端确认收到 `seq=0` 的命令。

---

### 4.5 输出方向的累计 ACK 语义

服务端向客户端发送 `TYPE_OUTPUT` 时，也会使用序列号。

客户端收到输出分片后，会回复 `TYPE_ACK`。当前实现中，输出 ACK 使用 Go-Back-N 的累计确认语义：

```text
ACK seq = 客户端已经按序接收的最后一个输出分片序列号
```

例如服务端发送：

```text
TYPE_OUTPUT seq=82
TYPE_OUTPUT seq=83
TYPE_OUTPUT seq=84
```

如果客户端已经按序收到 82、83、84，那么客户端可以回复：

```text
TYPE_ACK seq=84
```

表示：

```text
84 及之前的输出分片都已经按序收到
```

如果 83 丢失，而客户端后来收到了 84，客户端不会确认 84，而是继续确认最后一个按序收到的分片，例如：

```text
TYPE_ACK seq=82
```

这就是后退 N 帧 ARQ 的累计 ACK 行为。

---

### 4.6 序列号在重传中的作用

超时重传时，项目不会给重传包换新的序列号，而是复用原来的 seq。

例如客户端发送命令：

```text
55 54 01 00 00 00 00 ...
```

如果没有收到 ACK，客户端会重传：

```text
55 54 01 00 00 00 00 ...
55 54 01 00 00 00 00 ...
55 54 01 00 00 00 00 ...
```

判断重传的关键就是：

```text
报文类型相同，seq 相同，payload 相同，时间上间隔出现
```

服务端收到重复的命令 seq 后，可以判断它是重复包，只回复 ACK，不再次执行命令，从而避免重复执行带副作用的命令。

---

## 5. 所有 UDP 报文类型总览

项目中定义了 8 种 UDP 报文类型。

|   类型值 | 名称                   | 方向             | 作用                     |
| -------: | ---------------------- | ---------------- | ------------------------ |
| `0x01` | `TYPE_COMMAND`       | 客户端 → 服务端 | 发送普通命令             |
| `0x02` | `TYPE_OUTPUT`        | 服务端 → 客户端 | 返回命令输出             |
| `0x03` | `TYPE_ACK`           | 双向             | 确认收到某个报文         |
| `0x04` | `TYPE_HEARTBEAT`     | 客户端 → 服务端 | 心跳检测，表示客户端在线 |
| `0x05` | `TYPE_INTERRUPT`     | 客户端 → 服务端 | Ctrl+C 中断远程命令      |
| `0x06` | `TYPE_STDIN`         | 客户端 → 服务端 | PTY 模式下发送键盘输入   |
| `0x07` | `TYPE_RESIZE`        | 客户端 → 服务端 | 同步终端窗口大小         |
| `0x08` | `TYPE_WINDOW_UPDATE` | 客户端 → 服务端 | 通告接收窗口剩余容量     |

在 Wireshark 里可以通过 UDP Payload 第 3 个字节判断报文类型。

| UDP Payload 开头 | 含义               |
| ---------------- | ------------------ |
| `55 54 01`     | 普通命令包         |
| `55 54 02`     | 命令输出包         |
| `55 54 03`     | ACK 确认包         |
| `55 54 04`     | 心跳包             |
| `55 54 05`     | Ctrl+C 中断包      |
| `55 54 06`     | PTY 键盘输入包     |
| `55 54 07`     | 终端窗口大小同步包 |
| `55 54 08`     | 接收窗口通告包     |

---

## 6. `TYPE_COMMAND = 0x01`：普通命令包

### 5.1 作用

客户端把用户输入的普通命令发送给服务端。

例如用户输入：

```bash
pwd
```

客户端会发送一个 `TYPE_COMMAND` 报文。

### 5.2 方向

```text
客户端 → 服务端
```

### 5.3 Wireshark 特征

UDP Payload 开头：

```text
55 54 01
```

如果是第一条命令，序列号一般是 `0`，所以开头可能是：

```text
55 54 01 00 00 00 00
```

### 5.4 Payload 内容

Payload 是用户输入的命令内容。

例如：

```bash
pwd
```

对应 payload：

```text
70 77 64 0a
```

其中：

```text
70 = p
77 = w
64 = d
0a = 换行
```

### 5.5 服务端收到后做什么

服务端收到后会：

1. 校验魔数。
2. 判断类型为命令包。
3. 根据客户端 ID 找到客户端状态。
4. 根据序列号判断是否重复。
5. 先回复 ACK。
6. 执行命令。
7. 把命令输出通过 `TYPE_OUTPUT` 发回客户端。

---

## 7. `TYPE_OUTPUT = 0x02`：命令输出包

### 6.1 作用

服务端把命令执行结果发回客户端。

例如客户端执行：

```bash
pwd
```

服务端可能返回：

```text
/root/Remote-UDP-terminal
```

### 6.2 方向

```text
服务端 → 客户端
```

### 6.3 Wireshark 特征

UDP Payload 开头：

```text
55 54 02
```

### 6.4 Payload 内容

Payload 是命令输出内容，可能是标准输出，也可能是错误输出。

例如：

```text
/root/Remote-UDP-terminal\n
```

或者：

```text
bash: xxx: command not found\n
```

### 6.5 输出结束包

服务端发送完输出后，会发送一个特殊的 `TYPE_OUTPUT` 包，表示当前命令结束。

这个包的 payload 以：

```text
\0CWD:
```

开头。

对应十六进制：

```text
00 43 57 44 3a
```

例如：

```text
00 43 57 44 3a 2f 74 6d 70
```

表示：

```text
命令输出结束，当前目录是 /tmp
```

---

## 8. `TYPE_ACK = 0x03`：确认包

### 7.1 作用

ACK 表示：

```text
我已经收到某个序列号的报文了
```

ACK 是超时重传和可靠传输的关键。

### 7.2 方向

ACK 是双向的。

| 方向             | 含义                                      |
| ---------------- | ----------------------------------------- |
| 服务端 → 客户端 | 确认收到客户端命令、心跳、中断、Resize 等 |
| 客户端 → 服务端 | 确认收到服务端输出分片                    |

### 7.3 Wireshark 特征

UDP Payload 开头：

```text
55 54 03
```

### 7.4 ACK 序列号含义

ACK 里的序列号表示它确认的是哪个包。

例如客户端发送：

```text
TYPE_COMMAND seq=0
```

服务端回复：

```text
TYPE_ACK seq=0
```

表示：

```text
服务端已经收到 seq=0 的命令
```

### 7.5 普通 ACK

普通命令 ACK 通常 payload 为空。

数据长度字段通常是：

```text
00 00
```

### 7.6 心跳 ACK

心跳使用特殊序列号：

```text
0xFFFFFFFF
```

所以心跳 ACK 常见开头：

```text
55 54 03 FF FF FF FF
```

心跳 ACK 的 payload 通常包含提示符信息，以：

```text
\0PROMPT:
```

开头。

十六进制：

```text
00 50 52 4f 4d 50 54 3a
```

后面内容一般是：

```text
用户名\0主机名\0当前目录
```

### 7.7 输出 ACK 和窗口通告

客户端收到 `TYPE_OUTPUT` 后，会返回 ACK。

这个 ACK 可能携带窗口通告信息，payload 以：

```text
\0WIN:
```

开头。

十六进制：

```text
00 57 49 4e 3a
```

它表示：

```text
客户端还能接收多少输出分片和多少字节
```

---

## 9. `TYPE_HEARTBEAT = 0x04`：心跳包

### 8.1 作用

心跳包表示：

```text
客户端还在线
```

因为 UDP 是无连接协议，服务端不能像 TCP 那样自动知道客户端是否断开，所以需要心跳。

### 8.2 方向

```text
客户端 → 服务端
```

### 8.3 Wireshark 特征

心跳包开头：

```text
55 54 04 FF FF FF FF
```

其中：

```text
55 54       魔数
04          TYPE_HEARTBEAT
FF FF FF FF 心跳专用序列号
```

### 8.4 Payload 内容

心跳包 payload 通常为空。

所以心跳包一般只有 13 字节自定义协议头。

### 8.5 服务端收到后做什么

服务端收到心跳后：

1. 更新客户端最后活跃时间。
2. 记录客户端在线。
3. 返回 `TYPE_ACK`。
4. 在 ACK 里附带提示符信息。

一次完整心跳流程是：

```text
客户端 → 服务端：TYPE_HEARTBEAT
服务端 → 客户端：TYPE_ACK
```

---

## 10. `TYPE_INTERRUPT = 0x05`：中断包

### 9.1 作用

中断包用于远程 Ctrl+C。

当客户端按下 Ctrl+C 时，会发送 `TYPE_INTERRUPT` 给服务端，让服务端中断当前正在运行的命令。

例如中断：

```bash
ping 127.0.0.1
```

### 9.2 方向

```text
客户端 → 服务端
```

### 9.3 Wireshark 特征

UDP Payload 开头：

```text
55 54 05
```

### 9.4 Payload 内容

通常为空。

因为中断包只需要告诉服务端：

```text
中断当前客户端正在运行的进程
```

不需要携带命令内容。

### 9.5 服务端收到后做什么

服务端根据客户端 ID 找到该客户端正在运行的进程，然后发送中断信号。

Linux 下通常对应 `SIGINT`，效果类似在本地终端按 Ctrl+C。

---

## 11. `TYPE_STDIN = 0x06`：PTY 输入包

### 10.1 作用

这个包用于 PTY 交互模式。

普通命令模式下，客户端输入一整行命令，然后发送 `TYPE_COMMAND`。

但是进入 PTY 模式后，例如：

```bash
pty sh
```

或者：

```bash
pty top
```

用户的每次键盘输入都要实时发给服务端。这些输入使用 `TYPE_STDIN` 报文。

### 10.2 方向

```text
客户端 → 服务端
```

### 10.3 Wireshark 特征

UDP Payload 开头：

```text
55 54 06
```

### 10.4 Payload 内容

Payload 是用户键盘输入的原始字节。

例如在 PTY Shell 中输入：

```bash
echo PTY_OK
```

可能对应：

```text
65 63 68 6f 20 50 54 59 5f 4f 4b 0d
```

也就是：

```text
echo PTY_OK\r
```

如果按方向键，payload 可能是 ANSI 转义序列：

```text
1b 5b 41
```

表示方向键上。

如果在 `top` 中按 `q` 退出，payload 可能是：

```text
71
```

也就是字符 `q`。

### 10.5 服务端收到后做什么

服务端把 payload 写入远程 PTY，让远程程序像收到真实键盘输入一样处理。

---

## 12. `TYPE_RESIZE = 0x07`：终端窗口大小同步包

### 11.1 作用

这个包用于同步客户端终端窗口大小。

例如客户端终端大小是：

```text
40 行，120 列
```

客户端会发送 Resize 包给服务端，让远程 PTY 也使用类似的窗口大小。

这对 `top`、`vim` 这种全屏程序很重要。

### 11.2 方向

```text
客户端 → 服务端
```

### 11.3 Wireshark 特征

UDP Payload 开头：

```text
55 54 07
```

Payload 以：

```text
\0SZ:
```

开头。

十六进制：

```text
00 53 5a 3a
```

### 11.4 Payload 格式

Resize payload 格式是：

```text
00 53 5a 3a + rows 2 字节 + cols 2 字节
```

例如：

```text
00 53 5a 3a 00 28 00 78
```

解释：

```text
00 53 5a 3a = \0SZ:
00 28       = 40 行
00 78       = 120 列
```

所以这个包表示：

```text
终端大小为 40 行 × 120 列
```

### 11.5 服务端收到后做什么

服务端会：

1. 保存该客户端的终端行列数。
2. 如果客户端正在 PTY 模式中，更新远程 PTY 的窗口大小。
3. 返回 ACK。

---

## 13. `TYPE_WINDOW_UPDATE = 0x08`：接收窗口通告包

### 12.1 作用

这个包用于滑动窗口流量控制。

客户端告诉服务端：

```text
我现在还能接收多少输出分片，你不要发太快
```

这样可以防止服务端连续发送太多 UDP 输出包，导致客户端缓冲区压力过大。

### 12.2 方向

```text
客户端 → 服务端
```

### 12.3 Wireshark 特征

UDP Payload 开头：

```text
55 54 08
```

Payload 以：

```text
\0WIN:
```

开头。

十六进制：

```text
00 57 49 4e 3a
```

### 12.4 Payload 格式

Window Update payload 格式是：

```text
00 57 49 4e 3a + available_packets 2 字节 + available_bytes 4 字节
```

例如：

```text
00 57 49 4e 3a 00 08 00 00 b5 e0
```

解释：

```text
00 57 49 4e 3a = \0WIN:
00 08          = 还能接收 8 个分片
00 00 b5 e0    = 还能接收 46560 字节
```

### 12.5 服务端收到后做什么

服务端会更新该客户端的发送窗口。

如果客户端通告窗口变小，服务端就减少发送速度，避免发送过快。

---

## 14. 特殊 Payload 前缀总结

有些控制信息不是靠报文类型区分，而是放在 payload 里面，用特殊前缀表示。

| 前缀          | 十六进制                    | 出现位置                                       | 含义                         |
| ------------- | --------------------------- | ---------------------------------------------- | ---------------------------- |
| `\0CWD:`    | `00 43 57 44 3a`          | `TYPE_OUTPUT` payload                        | 命令输出结束，并携带当前目录 |
| `\0PROMPT:` | `00 50 52 4f 4d 50 54 3a` | `TYPE_ACK` payload                           | 服务端提示符信息             |
| `\0WIN:`    | `00 57 49 4e 3a`          | `TYPE_ACK` 或 `TYPE_WINDOW_UPDATE` payload | 接收窗口通告                 |
| `\0SZ:`     | `00 53 5a 3a`             | `TYPE_RESIZE` payload                        | 终端窗口大小                 |

---

## 15. 一次普通命令的 UDP 包流程

以执行：

```bash
pwd
```

为例。

### 14.1 客户端发送命令

```text
客户端 → 服务端
TYPE_COMMAND
```

Wireshark 开头：

```text
55 54 01 00 00 00 00
```

payload：

```text
pwd\n
```

### 14.2 服务端回复命令 ACK

```text
服务端 → 客户端
TYPE_ACK
```

Wireshark 开头：

```text
55 54 03 00 00 00 00
```

表示确认收到 `seq=0` 的命令。

### 14.3 服务端返回命令输出

```text
服务端 → 客户端
TYPE_OUTPUT
```

Wireshark 开头：

```text
55 54 02 00 00 00 00
```

payload 可能是：

```text
/root/Remote-UDP-terminal\n
```

### 14.4 客户端确认输出分片

```text
客户端 → 服务端
TYPE_ACK
```

Wireshark 开头：

```text
55 54 03 00 00 00 00
```

表示客户端已经收到服务端输出分片。

### 14.5 服务端发送输出结束包

```text
服务端 → 客户端
TYPE_OUTPUT
```

payload 开头：

```text
00 43 57 44 3a
```

表示：

```text
\0CWD:
```

说明当前命令输出结束，并携带当前工作目录。

---

## 16. 一次心跳的 UDP 包流程

### 15.1 客户端发送心跳

```text
客户端 → 服务端
TYPE_HEARTBEAT
```

Wireshark 开头：

```text
55 54 04 FF FF FF FF
```

### 15.2 服务端回复心跳 ACK

```text
服务端 → 客户端
TYPE_ACK
```

Wireshark 开头：

```text
55 54 03 FF FF FF FF
```

payload 通常以：

```text
00 50 52 4f 4d 50 54 3a
```

开头，也就是：

```text
\0PROMPT:
```

---

## 17. 一次超时重传在 Wireshark 中怎么看

如果连接错误端口：

```text
8.137.157.233:9998
```

客户端发送命令后收不到 ACK，就会重传同一个命令包。

Wireshark 中会看到多条类似报文：

```text
55 54 01 00 00 00 00 ...
55 54 01 00 00 00 00 ...
55 54 01 00 00 00 00 ...
55 54 01 00 00 00 00 ...
55 54 01 00 00 00 00 ...
```

判断重传的关键：

1. 类型都是 `01`，表示命令包。
2. 序列号都一样，例如都是 `00 00 00 00`。
3. payload 命令内容一样。
4. 中间没有对应的 `TYPE_ACK`。
5. 相邻发送时间间隔约等于 ACK 等待超时时间。

这说明客户端没有收到 ACK，于是重复发送同一个序列号的报文。

---

## 18. 一次 PTY 交互的 UDP 包流程

### 17.1 启动 PTY

用户输入：

```bash
pty sh
```

客户端先发送：

```text
TYPE_COMMAND
```

payload 是：

```text
pty sh\n
```

### 17.2 服务端回复 ACK

服务端返回：

```text
TYPE_ACK
```

### 17.3 PTY 模式下键盘输入

进入 PTY 后，用户输入不再按普通命令整行发送，而是通过 `TYPE_STDIN` 实时发送。

例如输入：

```bash
echo PTY_OK
```

Wireshark 中会看到：

```text
55 54 06 ...
```

payload 是键盘输入字节。

### 17.4 PTY 输出返回

服务端仍然通过：

```text
TYPE_OUTPUT
```

把 PTY 输出返回给客户端。

---

## 19. Wireshark 抓包过滤器

### 18.1 抓正常通信

抓取本机和服务器之间的 UDP 9999 通信：

```text
udp and host 8.137.157.233 and port 9999
```

显示过滤器：

```text
udp && ip.addr == 8.137.157.233 && udp.port == 9999
```

### 18.2 抓超时重传

如果用错误端口 `9998` 模拟服务端不可达，抓包过滤器：

```text
udp and host 8.137.157.233 and port 9998
```

显示过滤器：

```text
udp && ip.addr == 8.137.157.233 && udp.port == 9998
```

### 18.3 抓心跳

心跳包特征：

```text
55 54 04 FF FF FF FF
```

心跳 ACK 特征：

```text
55 54 03 FF FF FF FF
```

### 18.4 抓普通命令

普通命令包特征：

```text
55 54 01
```

服务端输出包特征：

```text
55 54 02
```

命令 ACK 特征：

```text
55 54 03
```

---

## 20. 最重要的抓包识别表

| UDP Payload 开头 | 报文含义           |
| ---------------- | ------------------ |
| `55 54 01`     | 客户端发送普通命令 |
| `55 54 02`     | 服务端返回命令输出 |
| `55 54 03`     | ACK 确认包         |
| `55 54 04`     | 心跳包             |
| `55 54 05`     | Ctrl+C 中断包      |
| `55 54 06`     | PTY 键盘输入包     |
| `55 54 07`     | 终端窗口大小同步包 |
| `55 54 08`     | 接收窗口通告包     |

| 特殊十六进制                | 含义                      |
| --------------------------- | ------------------------- |
| `FF FF FF FF`             | 心跳专用序列号            |
| `00 43 57 44 3a`          | `\0CWD:`，命令输出结束  |
| `00 50 52 4f 4d 50 54 3a` | `\0PROMPT:`，提示符信息 |
| `00 57 49 4e 3a`          | `\0WIN:`，窗口通告      |
| `00 53 5a 3a`             | `\0SZ:`，终端尺寸       |

---

## 21. Payload 字段详细解析补充

前面各章节已经按报文类型说明了 payload 的基本含义。本节集中从字节位置、长度范围、编码格式、结构组成、示例解析和异常处理角度，对本项目 UDP Payload 中的应用层数据段进行更完整的说明。

需要特别区分两个概念：

```text
UDP Payload = UDP 头部之后的全部数据，也就是 Wireshark 中 UDP Data 部分
项目 payload = 本项目 13 字节自定义协议头之后的数据段
```

也就是说，本项目在 RFC 768 定义的 UDP 数据区内部，又划分出：

```text
项目自定义协议头 + 项目 payload
```

完整层次如下：

```text
IP 头部
  ↓
UDP 头部，8 字节，RFC 768 定义
  ↓
UDP Payload / UDP Data
  ├─ 项目自定义协议头，13 字节
  └─ 项目 payload，0 到 1459 字节
```

---

### 20.1 Payload 在 UDP 报文中的位置

RFC 768 中 UDP 报文格式为：

```text
源端口 2 字节 | 目的端口 2 字节 | UDP 长度 2 字节 | UDP 校验和 2 字节 | UDP 数据
```

其中 UDP 头部固定为 8 字节。UDP 头部后面的 `UDP 数据` 就是 Wireshark 中显示的 UDP Payload / UDP Data。

本项目把 UDP 数据部分设计为：

```text
magic 2 字节 | type 1 字节 | seq 4 字节 | data_len 2 字节 | client_id 4 字节 | payload data_len 字节
```

因此，项目 payload 的位置是：

```text
UDP Payload 的第 14 个字节开始
```

如果按从 0 开始的偏移量计算：

| 区域          | UDP Payload 内偏移 |              长度 | 说明                 |
| ------------- | -----------------: | ----------------: | -------------------- |
| `magic`     |             0 到 1 |            2 字节 | 固定 `55 54`       |
| `type`      |                  2 |            1 字节 | 报文类型             |
| `seq`       |             3 到 6 |            4 字节 | 序列号               |
| `data_len`  |             7 到 8 |            2 字节 | 项目 payload 长度    |
| `client_id` |            9 到 12 |            4 字节 | 客户端 ID            |
| `payload`   |            13 开始 | `data_len` 字节 | 命令、输出或控制信息 |

例如：

```text
55 54 01 00 00 00 00 00 04 00 00 00 01 70 77 64 0a
```

其中前 13 字节是项目自定义协议头：

```text
55 54 01 00 00 00 00 00 04 00 00 00 01
```

从第 14 个字节开始的：

```text
70 77 64 0a
```

才是项目 payload。

---

### 20.2 Payload 字节长度范围

项目协议头中的 `data_len` 字段占 2 字节，理论上可以表示 `0` 到 `65535`。但是本项目为了避免超过常见以太网 MTU 后发生 IP 分片，主动限制了应用层 payload 最大长度。

代码中定义为：

```python
HEADER_SIZE = 13
MAX_DATA_SIZE = 1472 - HEADER_SIZE
```

所以：

```text
MAX_DATA_SIZE = 1472 - 13 = 1459 字节
```

因此本项目 payload 长度范围是：

```text
0 字节 <= payload 长度 <= 1459 字节
```

对应关系如下：

| 项目 payload 长度 | 项目 UDP Payload 总长度 | 含义                                                 |
| ----------------: | ----------------------: | ---------------------------------------------------- |
|                 0 |                 13 字节 | 只有项目协议头，没有数据段，例如普通 ACK、心跳、中断 |
|                 4 |                 17 字节 | 例如 `pwd\n` 命令                                  |
|                 8 |                 21 字节 | 例如 resize 控制 payload                             |
|                11 |                 24 字节 | 例如 window update 控制 payload                      |
|              1459 |               1472 字节 | 单个 UDP 包承载最大项目 payload                      |

注意 RFC 768 中 UDP Length 字段表示：

```text
UDP 头部 8 字节 + UDP 数据长度
```

所以如果项目 UDP Payload 总长度为 1472 字节，那么 RFC 768 中 UDP Length 通常为：

```text
8 + 1472 = 1480 字节
```

这符合 UDP 报文格式规范。本项目的 1472 字节限制不是 RFC 768 强制要求，而是为了适配常见 MTU，减少 IP 分片风险。

---

### 20.3 Payload 编码格式与结构组成

本项目 payload 没有统一成一种文本格式，而是根据报文类型采用不同编码方式。

总体规则如下：

| 报文类型               | payload 编码                 | 结构                                                                 |
| ---------------------- | ---------------------------- | -------------------------------------------------------------------- |
| `TYPE_COMMAND`       | UTF-8 文本字节               | 用户命令字符串，通常以 `\n` 结尾                                   |
| `TYPE_OUTPUT`        | UTF-8 输出字节或控制 payload | 普通输出文本，或 `\0CWD:` + 当前目录                               |
| `TYPE_ACK`           | 空 payload 或控制 payload    | 普通 ACK 为空；心跳 ACK 可带 `\0PROMPT:`；输出 ACK 可带 `\0WIN:` |
| `TYPE_HEARTBEAT`     | 空 payload                   | 不携带额外数据                                                       |
| `TYPE_INTERRUPT`     | 空 payload                   | 不携带额外数据                                                       |
| `TYPE_STDIN`         | 原始终端输入字节             | 按键、回车、方向键 ANSI 序列等                                       |
| `TYPE_RESIZE`        | 二进制控制结构               | `\0SZ:` + rows + cols                                              |
| `TYPE_WINDOW_UPDATE` | 二进制控制结构               | `\0WIN:` + available_packets + available_bytes                     |

本项目控制类 payload 通常以 `0x00` 开头，然后跟 ASCII 标识符。这样做的原因是普通文本输出一般不会以 NUL 字节开头，客户端和服务端可以快速区分普通内容和控制信息。

控制 payload 前缀如下：

| 前缀文本      | 十六进制                    | 后续结构                        | 用途         |
| ------------- | --------------------------- | ------------------------------- | ------------ |
| `\0CWD:`    | `00 43 57 44 3a`          | UTF-8 当前工作目录              | 输出结束标记 |
| `\0PROMPT:` | `00 50 52 4f 4d 50 54 3a` | `user\0host\0cwd\0session_id` | 提示符信息   |
| `\0WIN:`    | `00 57 49 4e 3a`          | 2 字节分片窗口 + 4 字节字节窗口 | 接收窗口通告 |
| `\0SZ:`     | `00 53 5a 3a`             | 2 字节 rows + 2 字节 cols       | 终端窗口大小 |

所有多字节整数均使用大端序，也就是网络字节序。

---

### 20.4 Payload 示例一：普通命令 payload

用户输入：

```bash
pwd
```

客户端发送 `TYPE_COMMAND` 报文，UDP Payload 十六进制可能是：

```text
55 54 01 00 00 00 00 00 04 00 00 00 01 70 77 64 0a
```

逐字段解析：

```text
55 54          magic = 0x5554
01             type = TYPE_COMMAND
00 00 00 00    seq = 0
00 04          data_len = 4
00 00 00 01    client_id = 1
70 77 64 0a    payload
```

payload 解析：

```text
70 = 'p'
77 = 'w'
64 = 'd'
0a = '\n'
```

所以 payload 文本内容是：

```text
pwd\n
```

服务端收到后，会把 payload 按 UTF-8 解码成命令字符串，然后执行 `pwd`。

---

### 20.5 Payload 示例二：输出结束 payload

服务端发送普通命令输出结束标记时，使用 `TYPE_OUTPUT`，payload 以 `\0CWD:` 开头。

示例 UDP Payload：

```text
55 54 02 00 00 00 05 00 09 00 00 00 01 00 43 57 44 3a 2f 74 6d 70
```

逐字段解析：

```text
55 54          magic = 0x5554
02             type = TYPE_OUTPUT
00 00 00 05    seq = 5
00 09          data_len = 9
00 00 00 01    client_id = 1
00 43 57 44 3a 2f 74 6d 70    payload
```

payload 解析：

```text
00 43 57 44 3a = \0CWD:
2f 74 6d 70    = /tmp
```

含义是：

```text
当前命令输出结束，服务端当前工作目录为 /tmp
```

客户端收到这个 payload 后不会把 `\0CWD:` 当普通输出显示，而是更新提示符目录，并设置命令完成状态。

---

### 20.6 Payload 示例三：ACK 中携带窗口通告 payload

客户端收到服务端输出分片后，会返回 `TYPE_ACK`，并在 ACK payload 中携带接收窗口通告。

示例 UDP Payload：

```text
55 54 03 00 00 00 82 00 0b 00 00 00 01 00 57 49 4e 3a 00 08 00 00 b5 e0
```

逐字段解析：

```text
55 54          magic = 0x5554
03             type = TYPE_ACK
00 00 00 82    seq = 130，表示累计确认到输出分片 130
00 0b          data_len = 11
00 00 00 01    client_id = 1
00 57 49 4e 3a 00 08 00 00 b5 e0    payload
```

payload 结构：

```text
00 57 49 4e 3a = \0WIN:
00 08          = available_packets = 8
00 00 b5 e0    = available_bytes = 46560
```

含义是：

```text
客户端已经按序收到 seq=130 及之前的输出分片；
同时客户端通告自己还能接收 8 个分片、46560 字节。
```

服务端收到后，会执行类似逻辑：

```text
实际发送窗口 = min(OUTPUT_WINDOW_SIZE, available_packets)
```

因此该 payload 用于滑动窗口流量控制。

---

### 20.7 Payload 示例四：终端窗口大小 payload

客户端同步终端尺寸时发送 `TYPE_RESIZE`。

示例 UDP Payload：

```text
55 54 07 00 00 00 10 00 08 00 00 00 01 00 53 5a 3a 00 28 00 78
```

逐字段解析：

```text
55 54          magic = 0x5554
07             type = TYPE_RESIZE
00 00 00 10    seq = 16
00 08          data_len = 8
00 00 00 01    client_id = 1
00 53 5a 3a 00 28 00 78    payload
```

payload 结构：

```text
00 53 5a 3a = \0SZ:
00 28       = rows = 40
00 78       = cols = 120
```

含义是：

```text
客户端当前终端大小为 40 行 × 120 列。
```

服务端收到后会保存该客户端的终端行列数。如果客户端正在 PTY 模式中，服务端还会同步调整远程 PTY 的窗口大小。

---

### 20.8 Payload 示例五：心跳 ACK 提示符 payload

客户端发送 `TYPE_HEARTBEAT` 后，服务端会回复 `TYPE_ACK`，并在 payload 中携带提示符信息。

示例 payload：

```text
00 50 52 4f 4d 50 54 3a 72 6f 6f 74 00 75 62 75 6e 74 75 00 2f 72 6f 6f 74 00 61 62 63 64
```

拆解：

```text
00 50 52 4f 4d 50 54 3a = \0PROMPT:
72 6f 6f 74                = root
00                         = 字段分隔符
75 62 75 6e 74 75          = ubuntu
00                         = 字段分隔符
2f 72 6f 6f 74             = /root
00                         = 字段分隔符
61 62 63 64                = abcd
```

含义是：

```text
user = root
host = ubuntu
cwd = /root
server_session_id = abcd
```

客户端收到后会更新命令提示符，例如：

```text
[root@ubuntu root]#
```

---

### 20.9 Payload 解析异常情况与处理方法

payload 解析过程中可能遇到以下异常情况。

| 异常情况                               | 表现                                      | 当前处理方法                                                        |
| -------------------------------------- | ----------------------------------------- | ------------------------------------------------------------------- |
| UDP 数据不足 13 字节                   | 连项目协议头都不完整                      | `unpack_msg()` 返回 `None`，直接丢弃                            |
| 魔数错误                               | 开头不是 `55 54`                        | `unpack_msg()` 返回 `None`，避免误解析其他 UDP 数据             |
| 报文类型非法                           | type 不在 `0x01` 到 `0x08` 定义集合中 | `unpack_msg()` 返回 `None`                                      |
| `data_len` 与实际 payload 长度不一致 | 声称长度和收到字节数不相等                | `unpack_msg()` 返回 `None`                                      |
| payload 超过 `MAX_DATA_SIZE`         | 发送端试图封装过大数据                    | `pack_msg()` 抛出 `ValueError`                                  |
| 控制 payload 前缀不匹配                | 例如 resize payload 不以 `\0SZ:` 开头   | 对应 `unpack_resize()` / `unpack_window_update()` 返回 `None` |
| 控制 payload 后续长度错误              | 例如 `\0WIN:` 后面不是 6 字节           | 返回 `None`，调用方忽略该控制信息                                 |
| resize 行列数异常                      | rows 或 cols 不在 `1..1000`             | `unpack_resize()` 返回 `None`                                   |
| UTF-8 文本无法严格解码                 | 输出或命令中有异常字节                    | 使用 `errors='replace'` 容错解码，避免程序崩溃                    |
| UDP 丢包、乱序或重复                   | payload 本身合法但到达顺序异常            | 依靠 seq、ACK、Go-Back-N 和超时重传处理                             |

其中最核心的安全边界是：

```python
if len(raw_data) != HEADER_SIZE + data_len:
    return None
```

这保证了项目不会在长度不一致时继续错误解析 payload。

对于控制类 payload，本项目采用“格式不匹配就忽略”的策略。例如窗口通告解析失败时，服务端不会崩溃，也不会错误修改发送窗口；Resize 解析失败时，服务端不会改变 PTY 尺寸。

---

### 20.10 与 RFC 768 的符合性说明

RFC 768 定义的是 UDP 传输层报文格式，包含：

```text
Source Port
Destination Port
Length
Checksum
Data
```

本项目没有改变 UDP 头部，也没有重新定义 UDP 的源端口、目的端口、长度或校验和字段。本项目所有自定义字段都放在 RFC 768 中的 `Data` 区域，也就是 UDP Payload 内部。

因此，本项目与 RFC 768 的关系可以概括为：

```text
RFC 768 负责 UDP 传输层格式；
本项目协议是承载在 UDP Data 中的应用层协议。
```

具体符合点如下：

| RFC 768 字段     | 标准含义                  | 本项目处理方式                              |
| ---------------- | ------------------------- | ------------------------------------------- |
| Source Port      | 发送端 UDP 端口           | 由操作系统 UDP socket 自动填写              |
| Destination Port | 接收端 UDP 端口           | 服务端默认监听 `9999`，客户端发送到该端口 |
| Length           | UDP 头部和 UDP 数据总长度 | 由操作系统根据实际发送字节数填写            |
| Checksum         | UDP 校验和                | 由操作系统网络协议栈处理                    |
| Data             | UDP 承载的数据            | 放入本项目 13 字节协议头和项目 payload      |

需要注意：

```text
RFC 768 不规定 UDP Data 内部必须是什么格式。
```

所以本项目在 UDP Data 内部定义 `magic/type/seq/data_len/client_id/payload`，属于应用层协议设计，不违反 UDP 标准。

同时，本项目将单个 UDP Data 控制在 1472 字节以内，是工程上的 MTU 优化：

```text
IPv4 常见 MTU 1500 字节
IPv4 头部通常 20 字节
UDP 头部固定 8 字节
可用 UDP Data 约 1472 字节
```

这有助于减少 IP 分片，提高抓包分析和传输稳定性，但 RFC 768 本身允许更大的 UDP 长度，最大由 16 位 Length 字段和下层 IP 限制共同决定。

---

## 22. 总结

本项目所有 UDP 包都遵循同一个自定义协议格式：

```text
55 54 + 类型 + 序列号 + 数据长度 + 客户端 ID + 数据段
```

其中：

```text
55 54
```

是固定魔数。

第 3 个字节表示报文类型：

```text
01 命令
02 输出
03 ACK
04 心跳
05 中断
06 PTY 输入
07 窗口大小
08 窗口通告
```

因此在 Wireshark 中，只要看到 UDP Payload 以 `55 54` 开头，就可以基本判断这是本项目的合法 UDP 报文；再看第 3 个字节，就可以知道这个包的具体作用。
