# UDP 远程终端项目超详细技术实现文档

本文档面向后续维护者和评审者，目标是让读者能够通过本文完整理解当前 UDP 远程终端项目的功能定位、源码实现、调用流程、关键算法、数据结构、技术难点、性能考虑以及边界情况处理。

特别说明：当前源码已经实现实验指导书中的基础功能、进阶功能和拔高功能。拔高部分包括基于接收端通告窗口的动态流量控制、终端窗口大小同步 WINCH/Resize，以及基于 PTY/ConPTY 的全屏交互式程序支持。Windows 环境已安装 `pywinpty`，并已通过 `pty cmd` 简化交互验证。本文按照当前真实源码状态说明实现细节。

---

## 目录

1. 项目整体架构
2. 功能概述
3. 协议层实现解析
4. UDP 基础通信实现解析
5. 命令方向停等 ARQ 实现解析
6. 输出方向滑动窗口可靠传输实现解析
7. 服务端命令执行实现解析
8. 实时输出实现解析
9. 多客户端支持实现解析
10. 心跳检测与离线清理实现解析
11. Ctrl+C 中断与控制字符处理实现解析
12. 异常处理实现解析
13. 测试验证逻辑解析
14. 拔高功能当前状态与技术方案分析
15. 模块交互关系与完整调用流程
16. 性能优化、边界情况与设计取舍
17. 总结

---

# 1. 项目整体架构

## 1.1 项目文件组成

当前项目主要文件如下：

```text
UDP/
├── common.py                         协议常量、封包解包、通用工具函数
├── client.py                         UDP 客户端，负责用户输入、可靠发送命令、显示输出
├── server.py                         UDP 服务端，负责接收命令、执行命令、可靠发送输出
├── test_all.py                       自动化测试脚本
├── README.md                         项目使用说明
├── plan-advance.md                   进阶功能设计说明
├── 基于UDP的远程终端实验指导书.md      实验任务要求
├── 实现细节说明.md                    普通详细实现说明
└── 超详细技术实现文档.md              本文档
```

## 1.2 三个核心源码文件职责

| 文件 | 定位 | 主要职责 |
|---|---|---|
| `common.py` | 协议公共层 | 定义报文格式、魔数、类型、序列号算法、ANSI 过滤、提示符封装 |
| `client.py` | 客户端应用层 | 读取用户命令、发送命令、等待 ACK、接收输出、心跳保活、中断远程命令 |
| `server.py` | 服务端应用层 | 绑定 UDP 端口、解析报文、管理客户端、执行命令、可靠返回输出、清理离线客户端 |

这三个文件的关系可以理解为：

```text
client.py  ←共同遵守→  common.py  ←共同遵守→  server.py
```

`common.py` 相当于双方的“协议说明书”。客户端和服务端都通过 `pack_msg()` 生成报文，通过 `unpack_msg()` 解析报文。

## 1.3 整体通信流程

完整流程如下：

```text
用户在客户端输入命令
        ↓
client.py 调用 _send_command_reliable()
        ↓
_send_reliable() 使用 pack_msg(TYPE_COMMAND, ...) 封装命令
        ↓
UDP socket.sendto() 发送到服务端
        ↓
server.py 的 _recv_loop() 使用 recvfrom() 收包
        ↓
unpack_msg() 校验协议头
        ↓
_handle_command() 检查序列号并回复 ACK
        ↓
服务端启动线程执行命令
        ↓
_execute_and_respond() 调用 subprocess.Popen()
        ↓
_stream_pipe() 实时读取 stdout/stderr
        ↓
_send_output_reliable() 分片输出
        ↓
_send_output_chunks_reliable() 用固定滑动窗口发送输出分片
        ↓
客户端 _recv_loop() 收到 TYPE_OUTPUT
        ↓
客户端回复 ACK，并通过 _handle_output_packet() 按序显示
        ↓
服务端发送输出结束标记
        ↓
客户端设置 output_done_event，显示下一条提示符
```

---

# 2. 功能概述

## 2.1 基础功能概述

基础功能是整个项目能运行起来的必要能力。

### 2.1.1 UDP 基础通信

定位：传输层通信基础。

作用：让客户端和服务端可以通过 UDP socket 发送和接收数据报。

使用场景：

- 客户端连接指定 IP 和端口。
- 服务端监听指定 UDP 端口。
- 客户端向服务端发送命令报文。
- 服务端向客户端返回执行结果。

### 2.1.2 自定义协议封包和解包

定位：应用层协议。

作用：在 UDP 原始字节流中加入自定义头部，让程序知道一个报文是什么类型、属于哪个客户端、序列号是多少、数据长度是多少。

使用场景：

- 识别命令报文。
- 识别输出报文。
- 识别 ACK 报文。
- 识别心跳报文。
- 丢弃非法报文。

### 2.1.3 停等 ARQ 可靠传输

定位：在 UDP 上模拟可靠传输。

作用：保证客户端发送的命令尽可能可靠地到达服务端，并避免服务端重复执行同一个命令。

使用场景：

- 命令报文丢失时自动重传。
- ACK 丢失时客户端重传。
- 服务端通过序列号避免重复执行。

### 2.1.4 基础终端命令执行

定位：远程终端核心业务。

作用：服务端收到命令后，在服务器本机执行命令，并把 stdout/stderr 返回给客户端。

使用场景：

- Windows 下执行 `dir`、`ipconfig`、`echo hello`。
- Linux/macOS 下执行 `ls`、`pwd`、`ps`、`ip addr`。
- 返回命令正常输出和错误输出。

### 2.1.5 多客户端支持

定位：服务端会话管理。

作用：服务端同时支持多个客户端，每个客户端拥有独立状态。

使用场景：

- 多个客户端同时连接同一个 UDP 服务端。
- 每个客户端有独立当前目录。
- 每个客户端有独立序列号。
- 一个客户端的命令输出不会发给另一个客户端。

### 2.1.6 心跳检测

定位：UDP 无连接环境下的在线状态维护。

作用：客户端定期告诉服务端自己还在线；服务端长期收不到心跳就清理客户端。

使用场景：

- 客户端启动时确认服务端可达。
- 客户端运行期间检测服务端是否断开。
- 服务端清理已经离线的客户端状态。

### 2.1.7 基本异常处理

定位：健壮性保障。

作用：避免非法报文、命令错误、网络中断、进程超时导致程序崩溃。

使用场景：

- 收到错误魔数报文时丢弃。
- 命令不存在时返回错误信息。
- 服务端不可达时客户端提示失败。
- 命令运行超时时杀掉进程。

## 2.2 进阶功能概述

进阶功能主要增强远程终端的可用性和传输效率。

### 2.2.1 终端控制字符增强

定位：改善远程终端交互体验。

作用：支持 Ctrl+C 中断、退格、Delete、回车换行规范化、Tab 透传、ANSI 序列过滤。

使用场景：

- 用户在客户端按 Ctrl+C 中断远程 `ping`。
- 用户输入中包含退格时，服务端收到的是修正后的命令。
- 输出中有颜色控制字符时过滤掉，避免乱码。

### 2.2.2 输出方向滑动窗口可靠传输

定位：提高大输出场景下的 UDP 传输效率。

作用：服务端返回大量输出时，不再完全停等，而是最多同时发送 8 个未确认分片。

使用场景：

- 命令输出超过单个 UDP 报文大小。
- `dir` 输出很多文件。
- `python -c "print('A' * 20000)"` 这类大输出命令。

### 2.2.3 实时命令输出

定位：改善长时间运行命令的用户体验。

作用：服务端不等待命令完全结束，而是实时读取 stdout/stderr 并发送给客户端。

使用场景：

- `ping` 这类逐步产生输出的命令。
- 长时间执行并持续打印进度的脚本。

## 2.3 拔高功能概述与当前状态

实验指导书中的拔高功能包括：

1. 动态滑动窗口流量控制。
2. 终端窗口大小同步 WINCH。
3. 支持 `top` / `vim` 类全屏交互式程序。

当前项目状态如下：

| 拔高功能 | 当前状态 | 说明 |
|---|---|---|
| 动态滑动窗口流量控制 | 已实现 | 客户端根据乱序接收缓冲区剩余分片数和剩余字节数生成窗口通告；服务端发送窗口取固定输出窗口和客户端通告窗口的较小值 |
| 终端窗口大小同步 | 已实现 | 客户端连接后、进入 PTY 前、窗口变化时发送 `TYPE_RESIZE`；服务端保存行列数并同步设置 PTY/ConPTY 大小 |
| top/vim 全屏交互程序 | 已实现 | 服务端普通命令继续使用 PIPE；显式 `pty <command>` 以及 `top`、`vim`、`vi`、`nano`、`less` 进入 PTY/ConPTY 路径，客户端 raw 输入按字节转发 |

需要注意：PTY 模式下输出不会过滤 ANSI 转义序列，因为全屏程序正是依靠清屏、移动光标、备用屏幕等控制序列完成界面渲染；普通命令路径仍然会过滤 ANSI，避免非交互式输出乱码。

---

# 3. 协议层实现解析

协议层位于 `common.py`。这是整个项目最基础的一层，因为所有网络报文都必须经过这里封装和解析。

## 3.1 协议常量完整代码

```python
import re
import struct

MAGIC = 0x5554
TYPE_COMMAND = 0x01
TYPE_OUTPUT = 0x02
TYPE_ACK = 0x03
TYPE_HEARTBEAT = 0x04
TYPE_INTERRUPT = 0x05
TYPE_STDIN = 0x06
TYPE_RESIZE = 0x07
TYPE_WINDOW_UPDATE = 0x08

VALID_TYPES = {
    TYPE_COMMAND,
    TYPE_OUTPUT,
    TYPE_ACK,
    TYPE_HEARTBEAT,
    TYPE_INTERRUPT,
    TYPE_STDIN,
    TYPE_RESIZE,
    TYPE_WINDOW_UPDATE,
}
HEADER_FORMAT = '>H B I H I'
HEADER_SIZE = struct.calcsize(HEADER_FORMAT)
MAX_DATA_SIZE = 1472 - HEADER_SIZE
MAX_SEQUENCE = 0xFFFFFFFF
DATA_SEQUENCE_MOD = MAX_SEQUENCE
HEARTBEAT_SEQ = MAX_SEQUENCE
OUTPUT_WINDOW_SIZE = 8
ACK_TIMEOUT = 0.5
MAX_RETRIES = 5
RECV_BUFFER_LIMIT_PACKETS = 32
RECV_BUFFER_LIMIT_BYTES = MAX_DATA_SIZE * RECV_BUFFER_LIMIT_PACKETS
FLOW_CONTROL_IDLE_TIMEOUT = 5
OUTPUT_DONE_PREFIX = b'\x00CWD:'
PROMPT_INFO_PREFIX = b'\x00PROMPT:'
WINDOW_UPDATE_PREFIX = b'\x00WIN:'
RESIZE_PREFIX = b'\x00SZ:'
ANSI_ESCAPE_RE = re.compile(
    rb'\x1b(?:[@-Z\\-_]|\[[0-?]*[ -/]*[@-~]|\][^\x07]*(?:\x07|\x1b\\))'
)
PTY_PREFIX = 'pty'
PTY_COMMANDS = {'top', 'vim', 'vi', 'nano', 'less'}
```

## 3.2 代码解析

### 3.2.1 `import re`

`re` 是 Python 正则表达式模块。

这里用于编译 ANSI 转义序列匹配规则，后续通过 `strip_ansi_sequences()` 过滤终端颜色、光标控制等特殊序列。

### 3.2.2 `import struct`

`struct` 用于把 Python 整数转换成二进制字节，也可以把二进制字节解析回整数。

网络传输时不能直接发送 Python 对象，需要把协议头字段编码成固定长度字节。`struct.pack()` 和 `struct.unpack()` 就是做这件事的。

### 3.2.3 `MAGIC = 0x5554`

魔数用于识别本项目的数据包。

如果服务端收到一个 UDP 报文，但是报文头里的魔数不是 `0x5554`，说明它不是本协议报文，直接丢弃。

这样可以避免误处理其他程序发到同一端口的 UDP 数据。

### 3.2.4 报文类型常量

```python
TYPE_COMMAND = 0x01
TYPE_OUTPUT = 0x02
TYPE_ACK = 0x03
TYPE_HEARTBEAT = 0x04
TYPE_INTERRUPT = 0x05
```

它们用于区分一个报文的用途。

| 类型 | 方向 | 用途 |
|---|---|---|
| `TYPE_COMMAND` | 客户端到服务端 | 用户输入的命令 |
| `TYPE_OUTPUT` | 服务端到客户端 | 命令输出内容 |
| `TYPE_ACK` | 双向 | 确认某个序列号已收到，也可以携带窗口通告 |
| `TYPE_HEARTBEAT` | 客户端到服务端 | 心跳保活 |
| `TYPE_INTERRUPT` | 客户端到服务端 | 请求中断当前远程进程 |
| `TYPE_STDIN` | 客户端到服务端 | PTY/ConPTY 交互模式下逐字节转发用户按键 |
| `TYPE_RESIZE` | 客户端到服务端 | 同步客户端终端窗口行列数 |
| `TYPE_WINDOW_UPDATE` | 客户端到服务端 | 主动通告客户端接收缓冲区剩余容量 |

### 3.2.5 `VALID_TYPES`

```python
VALID_TYPES = {
    TYPE_COMMAND,
    TYPE_OUTPUT,
    TYPE_ACK,
    TYPE_HEARTBEAT,
    TYPE_INTERRUPT,
    TYPE_STDIN,
    TYPE_RESIZE,
    TYPE_WINDOW_UPDATE,
}
```

这是一个集合，用来快速判断报文类型是否合法。

集合查找的平均时间复杂度是 O(1)，比列表逐个比较更适合做合法性校验。

### 3.2.6 `HEADER_FORMAT = '>H B I H I'`

这是协议头格式。

`>` 表示大端字节序，也就是网络字节序。

字段解释如下：

| 格式符 | 字节数 | 含义 | 项目中对应字段 |
|---|---:|---|---|
| `H` | 2 | unsigned short | 魔数 |
| `B` | 1 | unsigned char | 报文类型 |
| `I` | 4 | unsigned int | 序列号 |
| `H` | 2 | unsigned short | 数据长度 |
| `I` | 4 | unsigned int | 客户端 ID |

所以协议头总长度是：

```text
2 + 1 + 4 + 2 + 4 = 13 字节
```

### 3.2.7 `MAX_DATA_SIZE = 1472 - HEADER_SIZE`

以太网常见 MTU 是 1500 字节。IP 头通常 20 字节，UDP 头 8 字节，因此 UDP 数据部分最好不要超过：

```text
1500 - 20 - 8 = 1472 字节
```

本项目还要在 UDP 数据部分里放 13 字节自定义协议头，所以真正业务数据最大长度是：

```text
1472 - 13 = 1459 字节
```

这样设计是为了尽量避免 IP 分片。

### 3.2.8 序列号相关常量

```python
MAX_SEQUENCE = 0xFFFFFFFF
DATA_SEQUENCE_MOD = MAX_SEQUENCE
HEARTBEAT_SEQ = MAX_SEQUENCE
```

`MAX_SEQUENCE` 是 32 位无符号整数最大值。

项目把 `0xFFFFFFFF` 保留给心跳报文使用，所以普通数据序列号通过 `DATA_SEQUENCE_MOD` 在 `0` 到 `0xFFFFFFFE` 范围内循环。

### 3.2.9 可靠传输参数

```python
OUTPUT_WINDOW_SIZE = 8
ACK_TIMEOUT = 0.5
MAX_RETRIES = 5
```

| 参数 | 作用 |
|---|---|
| `OUTPUT_WINDOW_SIZE` | 服务端输出方向固定滑动窗口大小 |
| `ACK_TIMEOUT` | 输出分片等待 ACK 的超时时间 |
| `MAX_RETRIES` | 输出分片最大重传次数 |

## 3.3 序列号算法完整代码

```python
def next_data_seq(seq: int) -> int:
    return (seq + 1) % DATA_SEQUENCE_MOD


def is_sequence_ahead(seq: int, expected: int) -> bool:
    distance = (seq - expected) % DATA_SEQUENCE_MOD
    return 0 < distance < DATA_SEQUENCE_MOD // 2
```

## 3.4 序列号算法解析

### 3.4.1 `next_data_seq()`

这个函数计算下一个普通数据序列号。

```python
return (seq + 1) % DATA_SEQUENCE_MOD
```

使用取模是为了让序列号达到最大值后可以回绕。

例如：

```text
0 → 1 → 2 → ... → 0xFFFFFFFE → 0
```

这是一种环形序列号设计，常见于 TCP 这类协议。

### 3.4.2 `is_sequence_ahead()`

这个函数用于判断某个序列号是不是在期望序列号的“未来”。

```python
distance = (seq - expected) % DATA_SEQUENCE_MOD
return 0 < distance < DATA_SEQUENCE_MOD // 2
```

因为序列号会回绕，不能简单使用 `seq > expected` 判断。

例如，如果当前期望序列号接近最大值，下一个序列号可能回到 0。此时 0 从普通整数比较看比最大值小，但在环形序列空间里它其实是“未来”。

这个算法的核心思想是：

1. 计算从 `expected` 到 `seq` 的环形距离。
2. 如果距离大于 0 且小于序列空间一半，就认为 `seq` 是未来包。
3. 如果距离很大，说明它更可能是旧包。

这个设计和 TCP 判断序列号先后关系的思路类似。

## 3.5 ANSI 与控制字符处理完整代码

```python
def strip_ansi_sequences(data: bytes) -> bytes:
    return ANSI_ESCAPE_RE.sub(b'', data)


def normalize_command_input(text: str) -> str:
    text = strip_ansi_sequences(text.encode('utf-8', errors='replace')).decode('utf-8', errors='replace')
    text = text.replace('\r\n', '\n').replace('\r', '\n')
    result = []
    for ch in text:
        if ch in ('\b', '\x7f'):
            if result:
                result.pop()
        else:
            result.append(ch)
    return ''.join(result)
```

## 3.6 控制字符算法解析

### 3.6.1 `strip_ansi_sequences()`

这个函数通过正则表达式把 ANSI 转义序列删除。

调用关系：

```text
客户端显示输出 → strip_ansi_sequences()
服务端处理命令输入 → normalize_command_input() → strip_ansi_sequences()
```

用途：

- 避免颜色控制字符污染输出。
- 避免光标移动控制字符导致显示错乱。

### 3.6.2 `normalize_command_input()`

这个函数负责把用户输入转换成更干净的命令字符串。

逻辑块说明：

```python
text = strip_ansi_sequences(text.encode('utf-8', errors='replace')).decode('utf-8', errors='replace')
```

先把字符串编码成 UTF-8 字节，过滤 ANSI，再解码回字符串。

```python
text = text.replace('\r\n', '\n').replace('\r', '\n')
```

统一换行风格。Windows 常用 `\r\n`，Unix 常用 `\n`，终端回车可能是 `\r`。统一成 `\n` 后，后续处理更简单。

```python
result = []
```

使用列表作为可变字符栈。因为 Python 字符串不可变，如果频繁删除最后一个字符，用列表效率更高。

```python
for ch in text:
    if ch in ('\b', '\x7f'):
        if result:
            result.pop()
    else:
        result.append(ch)
```

这个算法可以叫“栈式退格处理”。

- 普通字符入栈。
- 退格或 Delete 时弹出栈顶。
- 最后把栈拼接成字符串。

时间复杂度是 O(n)，其中 n 是输入长度。

## 3.7 输出结束和提示符信息封装完整代码

```python
def pack_output_done(cwd: str) -> bytes:
    return OUTPUT_DONE_PREFIX + cwd.encode('utf-8')


def unpack_output_done(data: bytes) -> str | None:
    if data == b'':
        return ''
    if data.startswith(OUTPUT_DONE_PREFIX):
        return data[len(OUTPUT_DONE_PREFIX):].decode('utf-8', errors='replace')
    return None


def pack_prompt_info(user: str, host: str, cwd: str) -> bytes:
    return PROMPT_INFO_PREFIX + '\0'.join((user, host, cwd)).encode('utf-8')


def unpack_prompt_info(data: bytes) -> tuple[str, str, str] | None:
    if not data.startswith(PROMPT_INFO_PREFIX):
        return None
    parts = data[len(PROMPT_INFO_PREFIX):].decode('utf-8', errors='replace').split('\0', 2)
    if len(parts) != 3:
        return None
    return (parts[0], parts[1], parts[2])
```

### 3.7.1 输出结束标记设计

命令输出本身也是普通字节流。客户端需要知道什么时候一条命令结束，才能重新显示输入提示符。

项目使用特殊前缀：

```python
OUTPUT_DONE_PREFIX = b'\x00CWD:'
```

服务端命令结束后发送：

```python
pack_output_done(client.cwd)
```

客户端收到后调用：

```python
unpack_output_done(payload)
```

如果返回不是 `None`，说明这是输出结束标记，而不是普通输出。

### 3.7.2 提示符信息设计

心跳 ACK 中会携带：

1. 服务端用户名。
2. 服务端主机名。
3. 当前工作目录。

格式是：

```text
\x00PROMPT:user\0host\0cwd
```

使用 `\0` 分隔字段，是因为用户名、主机名和路径里通常不会包含空字符。

## 3.8 报文封包完整代码

```python
def pack_msg(msg_type: int, seq: int, client_id: int, data: bytes) -> bytes:
    if msg_type not in VALID_TYPES:
        raise ValueError("Invalid message type")
    if not 0 <= seq <= MAX_SEQUENCE:
        raise ValueError("Sequence number out of range")
    if not 0 <= client_id <= 0xFFFFFFFF:
        raise ValueError("Client ID out of range")
    if len(data) > MAX_DATA_SIZE:
        raise ValueError(f"Data size exceeds maximum of {MAX_DATA_SIZE} bytes")
    header = struct.pack(HEADER_FORMAT, MAGIC, msg_type, seq, len(data), client_id)
    return header + data
```

### 3.8.1 `pack_msg()` 逻辑解析

这个函数是所有发送路径都会调用的核心函数。

调用方包括：

| 调用位置 | 报文类型 |
|---|---|
| 客户端 `_send_reliable()` | `TYPE_COMMAND` |
| 客户端 `_send_heartbeat()` | `TYPE_HEARTBEAT` |
| 客户端 `_send_interrupt()` | `TYPE_INTERRUPT` |
| 客户端 `_recv_loop()` 收到输出后 | `TYPE_ACK` |
| 服务端 `_handle_command()` | `TYPE_ACK` |
| 服务端 `_handle_heartbeat()` | `TYPE_ACK` |
| 服务端 `_send_packet()` | `TYPE_OUTPUT` |

逐块说明：

```python
if msg_type not in VALID_TYPES:
    raise ValueError("Invalid message type")
```

防止调用者传入未定义类型。

```python
if not 0 <= seq <= MAX_SEQUENCE:
    raise ValueError("Sequence number out of range")
```

确保序列号能放进 4 字节无符号整数。

```python
if not 0 <= client_id <= 0xFFFFFFFF:
    raise ValueError("Client ID out of range")
```

客户端 ID 也是 4 字节无符号整数。

```python
if len(data) > MAX_DATA_SIZE:
    raise ValueError(...)
```

避免单个 UDP 报文过大。

```python
header = struct.pack(HEADER_FORMAT, MAGIC, msg_type, seq, len(data), client_id)
return header + data
```

把头部字段打包成 13 字节，然后拼接业务数据。

## 3.9 报文解包完整代码

```python
def unpack_msg(raw_data: bytes) -> tuple[int, int, int, bytes] | None:
    if len(raw_data) < HEADER_SIZE:
        return None
    magic, msg_type, seq, data_len, client_id = struct.unpack(HEADER_FORMAT, raw_data[:HEADER_SIZE])
    if magic != MAGIC or msg_type not in VALID_TYPES:
        return None
    if len(raw_data) != HEADER_SIZE + data_len:
        return None
    data = raw_data[HEADER_SIZE:]
    return (msg_type, seq, client_id, data)
```

### 3.9.1 `unpack_msg()` 逻辑解析

所有接收路径都会调用这个函数。

客户端调用位置：

```text
client.py → _recv_loop()
```

服务端调用位置：

```text
server.py → _recv_loop()
```

逐块说明：

```python
if len(raw_data) < HEADER_SIZE:
    return None
```

如果连协议头都不够，肯定不是合法报文。

```python
magic, msg_type, seq, data_len, client_id = struct.unpack(...)
```

把前 13 字节解析成 5 个字段。

```python
if magic != MAGIC or msg_type not in VALID_TYPES:
    return None
```

魔数或类型不合法，丢弃。

```python
if len(raw_data) != HEADER_SIZE + data_len:
    return None
```

检查实际数据长度是否等于头部声明的数据长度。

这一步可以防止截断包或伪造长度包被继续处理。

```python
data = raw_data[HEADER_SIZE:]
return (msg_type, seq, client_id, data)
```

返回业务层需要的字段。

---

# 4. UDP 基础通信实现解析

## 4.1 服务端初始化完整代码

```python
class UDPServer:
    def __init__(self, host='0.0.0.0', port=9999):
        self.sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
        self.sock.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
        self.sock.bind((host, port))
        self.clients = {}
        self.ack_events = {}
        self.ack_lock = threading.Lock()
        self.running = False
        self.encoding = locale.getpreferredencoding(False) or 'utf-8'
        self.server_user = getpass.getuser()
        self.server_host = platform.node() or socket.gethostname()
        print(f"UDP Server started on {host}:{port}")
```

## 4.2 服务端初始化解析

```python
self.sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
```

创建 IPv4 UDP socket。

- `AF_INET`：IPv4。
- `SOCK_DGRAM`：UDP 数据报。

```python
self.sock.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
```

允许地址复用。这样服务端异常退出后，端口更容易被重新绑定。

```python
self.sock.bind((host, port))
```

绑定监听地址和端口。默认 `0.0.0.0:9999`。

```python
self.clients = {}
```

客户端状态表。键是 `client_id`，值是 `ClientInfo`。

```python
self.ack_events = {}
```

输出方向滑动窗口使用的 ACK 等待表。键是 `(client_id, seq)`，值是 `threading.Event()`。

```python
self.ack_lock = threading.Lock()
```

保护 `ack_events` 的线程锁，因为接收线程和发送线程都会访问它。

## 4.3 客户端初始化完整代码

```python
class UDPClient:
    def __init__(self, server_host='127.0.0.1', server_port=9999):
        self.server_addr = (server_host, server_port)
        self.client_id = random.randint(1, 2**32 - 1)
        self.sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
        self.sock.settimeout(0.5)
        self.running = False
        self.send_seq = 0
        self.recv_expected_seq = 0
        self.recv_buffer = {}
        self.ack_event = threading.Event()
        self.output_done_event = threading.Event()
        self.heartbeat_ack_event = threading.Event()
        self.last_heartbeat_ack = 0
        self.waiting_seq = -1
```

## 4.4 客户端初始化解析

```python
self.server_addr = (server_host, server_port)
```

保存服务端地址。之后所有 `sendto()` 都发到这里。

```python
self.client_id = random.randint(1, 2**32 - 1)
```

生成随机客户端 ID，用于多客户端区分。

```python
self.sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
```

创建 UDP socket。

```python
self.sock.settimeout(0.5)
```

设置接收超时时间，避免 `recvfrom()` 永久阻塞，使线程可以定期检查 `self.running` 状态。

```python
self.send_seq = 0
```

客户端命令发送序列号。

```python
self.recv_expected_seq = 0
self.recv_buffer = {}
```

客户端接收服务端输出时使用。`recv_expected_seq` 是当前期望输出序列号，`recv_buffer` 缓存乱序到达的输出分片。

## 4.5 服务端接收循环完整代码

```python
def _recv_loop(self):
    while self.running:
        try:
            data, addr = self.sock.recvfrom(1500)
            msg = unpack_msg(data)
            if not msg:
                continue
            msg_type, seq, client_id, payload = msg

            if client_id not in self.clients:
                self.clients[client_id] = ClientInfo(addr)
                print(f"New client connected: {client_id} from {addr}")

            client = self.clients[client_id]
            client.addr = addr
            client.last_heartbeat = time.time()

            if msg_type == TYPE_HEARTBEAT:
                self._handle_heartbeat(client_id, seq, addr)
            elif msg_type == TYPE_ACK:
                self._handle_ack(client_id, seq)
            elif msg_type == TYPE_COMMAND:
                self._handle_command(client_id, seq, payload, addr)
            elif msg_type == TYPE_INTERRUPT:
                self._handle_interrupt(client_id)
        except Exception as e:
            if self.running:
                print(f"Recv error: {e}")
```

## 4.6 服务端接收流程解析

调用流程：

```text
UDPServer.start()
    ↓
启动线程执行 UDPServer._recv_loop()
    ↓
socket.recvfrom(1500)
    ↓
unpack_msg(data)
    ↓
根据 msg_type 分发到不同处理函数
```

关键点：

1. `recvfrom(1500)` 接收一个 UDP 数据报。
2. `unpack_msg(data)` 校验协议合法性。
3. 新客户端自动注册到 `self.clients`。
4. 每次收到报文都更新客户端地址和心跳时间。
5. 根据报文类型分发到不同处理函数。

使用的设计模式：分发器模式。

`_recv_loop()` 不直接处理所有业务细节，而是根据类型调用 `_handle_heartbeat()`、`_handle_ack()`、`_handle_command()`、`_handle_interrupt()`。这样代码结构更清晰。

---

# 5. 命令方向停等 ARQ 实现解析

## 5.1 算法概述

停等 ARQ 是最简单的可靠传输算法。

算法流程：

```text
发送一个报文
    ↓
等待 ACK
    ↓
收到 ACK：发送下一个
超时未收到 ACK：重发当前报文
重试次数超过上限：发送失败
```

当前项目中，客户端发送命令使用停等 ARQ。

## 5.2 客户端可靠发送完整代码

```python
def _send_reliable(self, data):
    max_retries = 5
    retry_count = 0
    self.waiting_seq = self.send_seq
    self.ack_event.clear()

    while retry_count < max_retries and self.running:
        try:
            msg = pack_msg(TYPE_COMMAND, self.send_seq, self.client_id, data)
            self.sock.sendto(msg, self.server_addr)
            if self.ack_event.wait(timeout=2):
                self.send_seq = next_data_seq(self.send_seq)
                self.waiting_seq = -1
                return True
            retry_count += 1
            with self.print_lock:
                print(f"\nTimeout waiting for ACK, retrying {retry_count}/{max_retries}")
        except Exception as e:
            with self.print_lock:
                print(f"\nSend error: {e}")
            retry_count += 1

    self.waiting_seq = -1
    with self.print_lock:
        print("\nSend failed after retries: server may be unreachable or network interrupted")
    return False
```

## 5.3 逐逻辑块解析

```python
max_retries = 5
retry_count = 0
```

最多重传 5 次，`retry_count` 记录当前已经重传多少次。

```python
self.waiting_seq = self.send_seq
self.ack_event.clear()
```

`waiting_seq` 表示当前正在等待哪个序列号的 ACK。

`ack_event` 是线程同步对象。接收线程收到对应 ACK 时会调用 `self.ack_event.set()`。

```python
msg = pack_msg(TYPE_COMMAND, self.send_seq, self.client_id, data)
```

调用协议层 `pack_msg()` 封装命令报文。

关键参数：

| 参数 | 值 | 含义 |
|---|---|---|
| `msg_type` | `TYPE_COMMAND` | 这是命令报文 |
| `seq` | `self.send_seq` | 当前命令序列号 |
| `client_id` | `self.client_id` | 当前客户端 ID |
| `data` | 命令字节 | 用户输入命令 |

```python
self.sock.sendto(msg, self.server_addr)
```

通过 UDP 发送到服务端。

```python
if self.ack_event.wait(timeout=2):
```

等待 ACK，最多等 2 秒。

如果接收线程收到对应 ACK，会设置 `ack_event`，这里返回 True。

```python
self.send_seq = next_data_seq(self.send_seq)
```

ACK 到达后，说明当前命令已经被服务端确认，客户端推进到下一个命令序列号。

```python
retry_count += 1
```

如果超时，就增加重传次数，然后循环重新发送同一个序列号的命令。

## 5.4 客户端 ACK 接收代码

```python
if msg_type == TYPE_ACK:
    prompt_info = unpack_prompt_info(payload)
    if prompt_info:
        self._apply_prompt_info(*prompt_info)
    if seq == HEARTBEAT_SEQ:
        self.last_heartbeat_ack = time.monotonic()
        self.heartbeat_ack_event.set()
    if seq == self.waiting_seq:
        self.ack_event.set()
```

解析：

1. 如果是 ACK 报文，先尝试解析提示符信息。
2. 如果 ACK 序列号是 `HEARTBEAT_SEQ`，说明这是心跳 ACK。
3. 如果 ACK 序列号等于 `waiting_seq`，说明这是当前命令的 ACK，于是唤醒 `_send_reliable()`。

这里体现了事件驱动思想：发送线程等待事件，接收线程负责设置事件。

## 5.5 服务端命令 ACK 与去重完整代码

```python
def _handle_command(self, client_id, seq, payload, addr):
    client = self.clients[client_id]

    if seq != client.recv_expected_seq:
        ack_msg = pack_msg(TYPE_ACK, seq, client_id, b'')
        self.sock.sendto(ack_msg, addr)
        return

    client.recv_expected_seq = next_data_seq(client.recv_expected_seq)
    ack_msg = pack_msg(TYPE_ACK, seq, client_id, b'')
    self.sock.sendto(ack_msg, addr)

    text = payload.decode('utf-8', errors='replace')
    cmd = normalize_command_input(text).rstrip('\r\n')
    if not cmd:
        return

    if self._is_cd_command(cmd):
        threading.Thread(
            target=self._change_directory_and_respond,
            args=(client_id, cmd),
            daemon=True
        ).start()
        return

    threading.Thread(
        target=self._execute_and_respond,
        args=(client_id, cmd),
        daemon=True
    ).start()
```

## 5.6 服务端去重算法解析

```python
if seq != client.recv_expected_seq:
```

如果收到的命令序列号不是当前期望值，说明可能是：

1. 旧命令重复到达。
2. ACK 丢失后客户端重传旧命令。
3. 乱序命令。

当前命令方向采用停等 ARQ，所以正常情况下不会出现未来命令。只要不是期望序列号，就不执行。

```python
ack_msg = pack_msg(TYPE_ACK, seq, client_id, b'')
self.sock.sendto(ack_msg, addr)
return
```

虽然不执行重复命令，但仍然回复 ACK。

这样可以解决 ACK 丢失问题：

```text
客户端发送 seq=0 命令
服务端执行并回复 ACK
ACK 丢失
客户端重发 seq=0
服务端发现 seq=0 已经执行过
服务端只回复 ACK，不再执行
客户端收到 ACK 后继续 seq=1
```

```python
client.recv_expected_seq = next_data_seq(client.recv_expected_seq)
```

如果是新命令，服务端推进期望序列号。

这个变量保存在每个客户端自己的 `ClientInfo` 里，所以多客户端之间互不影响。

---

# 6. 输出方向滑动窗口可靠传输实现解析

## 6.1 算法概述

服务端返回命令输出时使用固定大小滑动窗口。

它和停等 ARQ 的区别：

| 方案 | 特点 | 优点 | 缺点 |
|---|---|---|---|
| 停等 ARQ | 一次只发一个包 | 简单 | 大输出效率低 |
| 滑动窗口 | 一次允许多个未确认包 | 吞吐更高 | 状态管理更复杂 |

当前窗口大小固定为：

```python
OUTPUT_WINDOW_SIZE = 8
```

## 6.2 服务端输出分片完整代码

```python
def _send_output_reliable(self, client_id, data):
    chunks = [data[i:i + MAX_DATA_SIZE] for i in range(0, len(data), MAX_DATA_SIZE)]
    return self._send_output_chunks_reliable(client_id, chunks)
```

解析：

```python
chunks = [data[i:i + MAX_DATA_SIZE] for i in range(0, len(data), MAX_DATA_SIZE)]
```

这是列表推导式，用于把大输出切成多个分片。

例如数据长度是 4000 字节，`MAX_DATA_SIZE` 是 1459，那么会切成大约 3 个分片。

时间复杂度是 O(n)，n 是输出字节数。

空间复杂度也是 O(n)，因为生成了分片列表。

## 6.3 滑动窗口发送完整代码

```python
def _send_output_chunks_reliable(self, client_id, chunks):
    if not chunks:
        return True

    client = self.clients.get(client_id)
    if not client:
        return False

    with client.send_lock:
        addr = client.addr
        packets = []
        for chunk in chunks:
            seq = client.send_seq
            ack_event = threading.Event()
            with self.ack_lock:
                self.ack_events[(client_id, seq)] = ack_event
            packets.append({
                'seq': seq,
                'chunk': chunk,
                'event': ack_event,
                'sent': False,
                'acked': False,
                'retries': 0,
                'last_sent': 0.0,
            })
            client.send_seq = next_data_seq(client.send_seq)

        base = 0
        next_to_send = 0
        try:
            while base < len(packets) and self.running:
                while next_to_send < len(packets) and next_to_send - base < OUTPUT_WINDOW_SIZE:
                    packet = packets[next_to_send]
                    self._send_packet(client_id, addr, packet['seq'], packet['chunk'])
                    packet['sent'] = True
                    packet['last_sent'] = time.monotonic()
                    next_to_send += 1

                for packet in packets[base:next_to_send]:
                    if not packet['acked'] and packet['event'].is_set():
                        packet['acked'] = True

                while base < len(packets) and packets[base]['acked']:
                    with self.ack_lock:
                        self.ack_events.pop((client_id, packets[base]['seq']), None)
                    base += 1

                if base >= len(packets):
                    return True

                now = time.monotonic()
                timed_out = any(
                    not packet['acked'] and packet['sent'] and now - packet['last_sent'] >= ACK_TIMEOUT
                    for packet in packets[base:next_to_send]
                )
                if not timed_out:
                    time.sleep(0.01)
                    continue

                for packet in packets[base:next_to_send]:
                    if packet['acked']:
                        continue
                    if packet['retries'] >= MAX_RETRIES:
                        print(f"Failed to send to client {client_id} after {MAX_RETRIES} retries")
                        return False
                    packet['retries'] += 1
                    self._send_packet(client_id, addr, packet['seq'], packet['chunk'])
                    packet['last_sent'] = time.monotonic()
                    print(f"Retry {packet['retries']}/{MAX_RETRIES} for client {client_id} seq {packet['seq']}")
        except Exception as e:
            print(f"Send error: {e}")
            return False
        finally:
            with self.ack_lock:
                for packet in packets:
                    self.ack_events.pop((client_id, packet['seq']), None)

    return False
```

## 6.4 关键数据结构解析

### 6.4.1 `packets` 列表

每个输出分片会被包装成一个字典：

```python
{
    'seq': seq,
    'chunk': chunk,
    'event': ack_event,
    'sent': False,
    'acked': False,
    'retries': 0,
    'last_sent': 0.0,
}
```

字段含义：

| 字段 | 含义 |
|---|---|
| `seq` | 该输出分片的序列号 |
| `chunk` | 实际要发送的字节数据 |
| `event` | 等待 ACK 的事件对象 |
| `sent` | 是否已经发送过 |
| `acked` | 是否已经收到 ACK |
| `retries` | 已重传次数 |
| `last_sent` | 上次发送时间 |

### 6.4.2 `ack_events` 字典

服务端全局维护：

```python
self.ack_events[(client_id, seq)] = ack_event
```

当服务端接收线程收到 ACK 时，会通过 `(client_id, seq)` 找到对应事件并设置。

这样发送线程和接收线程之间就通过事件对象完成同步。

### 6.4.3 `base` 和 `next_to_send`

```python
base = 0
next_to_send = 0
```

这是滑动窗口算法的两个核心指针。

| 变量 | 含义 |
|---|---|
| `base` | 当前窗口左边界，也就是最早未确认分片 |
| `next_to_send` | 下一个尚未发送的新分片 |

窗口中的分片范围是：

```text
[base, next_to_send)
```

窗口还能继续发送的条件是：

```python
next_to_send - base < OUTPUT_WINDOW_SIZE
```

## 6.5 滑动窗口逻辑块解析

### 6.5.1 分片初始化

```python
for chunk in chunks:
    seq = client.send_seq
    ack_event = threading.Event()
    with self.ack_lock:
        self.ack_events[(client_id, seq)] = ack_event
    packets.append(...)
    client.send_seq = next_data_seq(client.send_seq)
```

每个分片分配一个序列号，并创建一个等待 ACK 的事件。

### 6.5.2 填满窗口

```python
while next_to_send < len(packets) and next_to_send - base < OUTPUT_WINDOW_SIZE:
    packet = packets[next_to_send]
    self._send_packet(client_id, addr, packet['seq'], packet['chunk'])
    packet['sent'] = True
    packet['last_sent'] = time.monotonic()
    next_to_send += 1
```

只要窗口没满，就不断发送新分片。

这里调用的关键函数是：

```python
self._send_packet(client_id, addr, packet['seq'], packet['chunk'])
```

它内部会调用 `pack_msg(TYPE_OUTPUT, ...)` 封装输出报文，再用 UDP 发送。

### 6.5.3 检查 ACK

```python
for packet in packets[base:next_to_send]:
    if not packet['acked'] and packet['event'].is_set():
        packet['acked'] = True
```

如果某个分片对应的事件已经被接收线程设置，说明 ACK 到达，于是标记为已确认。

### 6.5.4 推动窗口左边界

```python
while base < len(packets) and packets[base]['acked']:
    with self.ack_lock:
        self.ack_events.pop((client_id, packets[base]['seq']), None)
    base += 1
```

只有窗口最左边的分片被确认后，窗口才能向右滑动。

如果中间某个包确认了，但最左边还没确认，窗口不能越过最左边未确认包。

### 6.5.5 超时检测

```python
timed_out = any(
    not packet['acked'] and packet['sent'] and now - packet['last_sent'] >= ACK_TIMEOUT
    for packet in packets[base:next_to_send]
)
```

检查窗口内是否存在超时未确认分片。

### 6.5.6 重传逻辑

```python
for packet in packets[base:next_to_send]:
    if packet['acked']:
        continue
    if packet['retries'] >= MAX_RETRIES:
        return False
    packet['retries'] += 1
    self._send_packet(...)
```

当前实现会重传窗口内所有未确认分片。

这更接近 Go-Back-N 的思想，但又不是严格的 Go-Back-N，因为客户端会缓存乱序分片，服务端也会根据单个 ACK 标记分片状态。因此它更像是“固定窗口 + 选择性 ACK 标记 + 超时批量重传”的混合方案。

## 6.6 客户端输出 ACK 与乱序缓存完整代码

```python
elif msg_type == TYPE_OUTPUT:
    ack_msg = pack_msg(TYPE_ACK, seq, self.client_id, b'')
    self.sock.sendto(ack_msg, self.server_addr)
    self._handle_output_packet(seq, payload)
```

```python
def _handle_output_packet(self, seq, payload):
    if seq == self.recv_expected_seq:
        self._process_output_payload(payload)
        self.recv_expected_seq = next_data_seq(self.recv_expected_seq)
        while self.recv_expected_seq in self.recv_buffer:
            buffered_payload = self.recv_buffer.pop(self.recv_expected_seq)
            self._process_output_payload(buffered_payload)
            self.recv_expected_seq = next_data_seq(self.recv_expected_seq)
    elif is_sequence_ahead(seq, self.recv_expected_seq):
        self.recv_buffer.setdefault(seq, payload)
```

## 6.7 客户端乱序处理算法解析

客户端收到输出分片后，先回复 ACK。这样即使后续显示处理出问题，也能减少服务端不必要重传。

然后进入 `_handle_output_packet()`。

### 6.7.1 正好是期望序列号

```python
if seq == self.recv_expected_seq:
```

如果收到的就是当前期望分片，则立即显示。

```python
self._process_output_payload(payload)
self.recv_expected_seq = next_data_seq(self.recv_expected_seq)
```

显示后推进期望序列号。

```python
while self.recv_expected_seq in self.recv_buffer:
```

检查之前缓存的乱序包中，是否已经有后续连续分片。如果有，就连续取出并显示。

### 6.7.2 是未来分片

```python
elif is_sequence_ahead(seq, self.recv_expected_seq):
    self.recv_buffer.setdefault(seq, payload)
```

如果分片序列号在未来，就先缓存。

`setdefault()` 的作用是：如果这个序列号已经缓存过，就不覆盖。这样可以避免重复包覆盖已有数据。

### 6.7.3 是旧分片

如果既不是期望序列号，也不是未来序列号，就说明它可能是旧包或重复包。代码不会处理它，但前面已经回复了 ACK。

## 6.8 性能考虑

1. 窗口大小为 8，比停等 ARQ 对大输出更高效。
2. 每个包单独 ACK，可以更快释放已确认分片。
3. 客户端支持乱序缓存，减少不必要丢弃。
4. 单包大小控制在 MTU 内，降低 IP 分片风险。
5. 发送循环中 `time.sleep(0.01)` 避免 CPU 空转。

---

# 7. 服务端命令执行实现解析

## 7.1 命令执行完整代码

```python
def _execute_and_respond(self, client_id, cmd):
    client = self.clients.get(client_id)
    if not client:
        return

    proc = None
    reader_threads = []
    timed_out = False
    try:
        if os.name == 'nt':
            proc = subprocess.Popen(
                ['cmd.exe', '/c', cmd],
                stdout=subprocess.PIPE,
                stderr=subprocess.PIPE,
                cwd=client.cwd,
                creationflags=subprocess.CREATE_NEW_PROCESS_GROUP
            )
        else:
            proc = subprocess.Popen(
                ['bash', '-c', cmd],
                stdout=subprocess.PIPE,
                stderr=subprocess.PIPE,
                cwd=client.cwd,
                preexec_fn=os.setsid
            )

        with client.process_lock:
            client.current_process = proc

        for pipe in (proc.stdout, proc.stderr):
            if pipe is None:
                continue
            thread = threading.Thread(target=self._stream_pipe, args=(client_id, pipe), daemon=True)
            thread.start()
            reader_threads.append(thread)

        try:
            proc.wait(timeout=30)
        except subprocess.TimeoutExpired:
            timed_out = True
            self._kill_process_tree(proc)
            try:
                proc.wait(timeout=2)
            except subprocess.TimeoutExpired:
                pass

        for thread in reader_threads:
            thread.join()

        if timed_out:
            self._send_output_reliable(client_id, b"Error: Command execution timed out\n")
    except Exception as e:
        self._send_output_reliable(client_id, f"Error: {str(e)}\n".encode('utf-8'))
    finally:
        if proc is not None:
            with client.process_lock:
                if client.current_process == proc:
                    client.current_process = None
        if client_id in self.clients:
            self._send_output_done(client_id)
```

## 7.2 逻辑块解析

### 7.2.1 获取客户端状态

```python
client = self.clients.get(client_id)
if not client:
    return
```

如果客户端已经被清理，就不再执行命令。

### 7.2.2 Windows 命令执行

```python
proc = subprocess.Popen(
    ['cmd.exe', '/c', cmd],
    stdout=subprocess.PIPE,
    stderr=subprocess.PIPE,
    cwd=client.cwd,
    creationflags=subprocess.CREATE_NEW_PROCESS_GROUP
)
```

关键参数：

| 参数 | 作用 |
|---|---|
| `cmd.exe /c` | 执行命令后退出 |
| `stdout=subprocess.PIPE` | 捕获标准输出 |
| `stderr=subprocess.PIPE` | 捕获错误输出 |
| `cwd=client.cwd` | 在客户端自己的当前目录中执行 |
| `CREATE_NEW_PROCESS_GROUP` | 创建新进程组，便于 Ctrl+C 中断 |

### 7.2.3 Linux/macOS 命令执行

```python
proc = subprocess.Popen(
    ['bash', '-c', cmd],
    stdout=subprocess.PIPE,
    stderr=subprocess.PIPE,
    cwd=client.cwd,
    preexec_fn=os.setsid
)
```

`bash -c` 表示让 bash 执行一条命令字符串。

`preexec_fn=os.setsid` 用于创建新的进程会话，使后续可以通过 `os.killpg()` 给整个进程组发送信号。

### 7.2.4 保存当前进程

```python
with client.process_lock:
    client.current_process = proc
```

这个变量用于 Ctrl+C 中断和客户端离线清理。

因为接收线程、执行线程、清理线程都可能访问 `current_process`，所以需要 `process_lock`。

### 7.2.5 启动输出读取线程

```python
for pipe in (proc.stdout, proc.stderr):
    thread = threading.Thread(target=self._stream_pipe, args=(client_id, pipe), daemon=True)
    thread.start()
```

stdout 和 stderr 分别用独立线程读取，避免其中一个管道阻塞导致另一个管道无法读取。

### 7.2.6 命令超时

```python
proc.wait(timeout=30)
```

最多等待 30 秒。

如果超时：

```python
timed_out = True
self._kill_process_tree(proc)
```

杀掉进程树，避免服务端资源被长期占用。

### 7.2.7 等待读取线程结束

```python
for thread in reader_threads:
    thread.join()
```

确保 stdout/stderr 中剩余内容都被读取并发送。

### 7.2.8 finally 清理

```python
if proc is not None:
    with client.process_lock:
        if client.current_process == proc:
            client.current_process = None
```

清除当前进程状态。

```python
self._send_output_done(client_id)
```

发送输出结束标记，通知客户端命令结束。

## 7.3 cd 命令特殊处理完整代码

```python
def _is_cd_command(self, cmd):
    tokens = self._split_command(cmd)
    return bool(tokens) and tokens[0].lower() == 'cd' and not any(token in {'&', '&&', '|', '||', ';'} for token in tokens)
```

```python
def _change_directory_and_respond(self, client_id, cmd):
    client = self.clients.get(client_id)
    if not client:
        return

    tokens = self._split_command(cmd)
    args = tokens[1:]
    if os.name == 'nt' and args and args[0].lower() == '/d':
        args = args[1:]

    if not args:
        target = os.path.expanduser('~')
    else:
        target = ' '.join(args).strip('"\'')
        target = os.path.expandvars(os.path.expanduser(target))
        if not os.path.isabs(target):
            target = os.path.join(client.cwd, target)

    try:
        new_cwd = os.path.abspath(target)
        if not os.path.isdir(new_cwd):
            output = f"cd: no such file or directory: {target}\n"
        else:
            client.cwd = new_cwd
            output = ''
    except Exception as e:
        output = f"cd: {e}\n"

    if client_id in self.clients:
        self._send_reliable(client_id, output.encode('utf-8'))
```

### 7.3.1 为什么 cd 需要特殊处理

`cd` 通常是 shell 内置命令。如果通过子进程执行：

```text
cmd.exe /c cd ..
```

或：

```text
bash -c "cd .."
```

目录变化只发生在子进程里，子进程退出后不会影响服务端主进程，也不会影响下一条命令。

所以项目把 `cd` 作为服务端内置命令处理，直接修改 `client.cwd`。

### 7.3.2 `_is_cd_command()` 解析

```python
tokens = self._split_command(cmd)
```

先用 `shlex.split()` 或普通 `split()` 分割命令。

```python
bool(tokens) and tokens[0].lower() == 'cd'
```

判断第一个 token 是否是 `cd`。

```python
not any(token in {'&', '&&', '|', '||', ';'} for token in tokens)
```

如果命令包含管道或命令连接符，就不按内置 `cd` 处理，避免误判复杂 shell 命令。

### 7.3.3 `_change_directory_and_respond()` 解析

核心处理：

1. 提取 `cd` 后面的路径。
2. Windows 下兼容 `cd /d`。
3. 无参数时切换到用户主目录。
4. 展开环境变量和 `~`。
5. 相对路径基于当前客户端 `client.cwd` 转成绝对路径。
6. 判断目标是否存在且是目录。
7. 成功则更新 `client.cwd`。
8. 失败则返回错误输出。
9. 最后发送输出和结束标记。

---

# 8. 实时输出实现解析

## 8.1 实时读取完整代码

```python
def _stream_pipe(self, client_id, pipe):
    try:
        utf8_decoder = codecs.getincrementaldecoder('utf-8')(errors='strict')
        local_decoder = codecs.getincrementaldecoder(self.encoding)(errors='replace')
        use_utf8 = True
        read_chunk = pipe.read1 if hasattr(pipe, 'read1') else pipe.read
        while self.running:
            chunk = read_chunk(4096)
            if not chunk:
                break
            if use_utf8:
                pending = utf8_decoder.getstate()[0]
                try:
                    text = utf8_decoder.decode(chunk)
                except UnicodeDecodeError:
                    use_utf8 = False
                    text = local_decoder.decode(pending + chunk)
            else:
                text = local_decoder.decode(chunk)
            if text and not self._send_output_reliable(client_id, text.encode('utf-8')):
                break
        if use_utf8:
            pending = utf8_decoder.getstate()[0]
            try:
                tail = utf8_decoder.decode(b'', final=True)
            except UnicodeDecodeError:
                tail = local_decoder.decode(pending, final=True)
        else:
            tail = local_decoder.decode(b'', final=True)
        if tail:
            self._send_output_reliable(client_id, tail.encode('utf-8'))
    except Exception as e:
        if self.running:
            print(f"Stream error: {e}")
    finally:
        try:
            pipe.close()
        except Exception:
            pass
```

## 8.2 逻辑解析

### 8.2.1 增量解码器

```python
utf8_decoder = codecs.getincrementaldecoder('utf-8')(errors='strict')
local_decoder = codecs.getincrementaldecoder(self.encoding)(errors='replace')
```

命令输出是字节流，可能包含中文。UTF-8 中文字符通常占多个字节。如果一次读取刚好切在字符中间，直接解码可能失败。

增量解码器可以处理这种情况：它会暂存不完整字符，等后续字节到达后再解码。

### 8.2.2 UTF-8 优先，本地编码兜底

```python
use_utf8 = True
```

项目优先尝试 UTF-8 解码。

如果出现 `UnicodeDecodeError`：

```python
use_utf8 = False
text = local_decoder.decode(pending + chunk)
```

切换到系统本地编码，例如 Windows 中文环境可能是 GBK 或 CP936。

这个策略提高了跨平台输出兼容性。

### 8.2.3 实时读取

```python
chunk = read_chunk(4096)
if not chunk:
    break
```

每次读取最多 4096 字节。如果读到空字节，说明管道结束。

### 8.2.4 实时发送

```python
if text and not self._send_output_reliable(client_id, text.encode('utf-8')):
    break
```

只要解码出文本，就立刻发送给客户端。

这里再次编码成 UTF-8，是为了让网络传输统一使用 UTF-8。

### 8.2.5 尾部刷新

```python
tail = utf8_decoder.decode(b'', final=True)
```

命令结束时，增量解码器可能还有缓存的尾部字符。这里把尾部刷新出来，避免丢最后几个字节。

---

# 9. 多客户端支持实现解析

## 9.1 客户端状态类完整代码

```python
class ClientInfo:
    def __init__(self, addr):
        self.addr = addr
        self.last_heartbeat = time.time()
        self.recv_expected_seq = 0
        self.send_seq = 0
        self.cwd = os.getcwd()
        self.current_process = None
        self.process_lock = threading.Lock()
        self.send_lock = threading.Lock()
```

## 9.2 字段逐项解析

| 字段 | 类型 | 用途 |
|---|---|---|
| `addr` | tuple | 客户端 UDP 地址，服务端回复时使用 |
| `last_heartbeat` | float | 最近一次收到该客户端报文的时间 |
| `recv_expected_seq` | int | 服务端期待该客户端下一条命令序列号 |
| `send_seq` | int | 服务端向该客户端发送输出时的序列号 |
| `cwd` | str | 该客户端独立当前工作目录 |
| `current_process` | Popen 或 None | 该客户端当前正在执行的进程 |
| `process_lock` | Lock | 保护 `current_process` |
| `send_lock` | Lock | 防止同一客户端多个输出发送流程交叉 |

## 9.3 多客户端注册逻辑

```python
if client_id not in self.clients:
    self.clients[client_id] = ClientInfo(addr)
    print(f"New client connected: {client_id} from {addr}")
```

服务端第一次看到某个 `client_id` 时创建 `ClientInfo`。

之后：

```python
client = self.clients[client_id]
client.addr = addr
client.last_heartbeat = time.time()
```

每次收到报文都更新地址和活跃时间。

## 9.4 多客户端隔离机制

隔离依赖以下设计：

1. 每个报文携带 `client_id`。
2. 服务端用 `self.clients[client_id]` 找到对应状态。
3. 每个客户端有独立 `recv_expected_seq`。
4. 每个客户端有独立 `send_seq`。
5. 每个客户端有独立 `cwd`。
6. 每个客户端有独立 `current_process`。

因此客户端 A 的 `cd` 不会影响客户端 B，客户端 A 的输出序列号也不会和客户端 B 混用。

---

# 10. 心跳检测与离线清理实现解析

## 10.1 客户端心跳完整代码

```python
def _send_heartbeat(self):
    try:
        msg = pack_msg(TYPE_HEARTBEAT, HEARTBEAT_SEQ, self.client_id, b'')
        self.sock.sendto(msg, self.server_addr)
        return True
    except Exception:
        return False


def _wait_for_heartbeat_ack(self, timeout=2):
    self.heartbeat_ack_event.clear()
    sent_at = time.monotonic()
    if not self._send_heartbeat():
        return False

    deadline = sent_at + timeout
    while self.running:
        if self.last_heartbeat_ack >= sent_at:
            return True
        remaining = deadline - time.monotonic()
        if remaining <= 0:
            return False
        self.heartbeat_ack_event.wait(remaining)
    return False


def _heartbeat_loop(self):
    while self.running:
        time.sleep(5)
        if self.running and not self._wait_for_heartbeat_ack(timeout=2):
            self._handle_connection_error()
            break
```

## 10.2 客户端心跳解析

```python
pack_msg(TYPE_HEARTBEAT, HEARTBEAT_SEQ, self.client_id, b'')
```

心跳报文没有业务数据，payload 是空字节。

`HEARTBEAT_SEQ` 是特殊序列号，用于和普通命令 ACK 区分。

```python
sent_at = time.monotonic()
```

记录发送时间。`monotonic()` 不受系统时间修改影响，适合计算超时。

```python
if self.last_heartbeat_ack >= sent_at:
    return True
```

如果收到的心跳 ACK 时间晚于发送时间，说明这次心跳成功。

```python
time.sleep(5)
```

客户端每 5 秒发送一次心跳。

## 10.3 服务端心跳完整代码

```python
def _handle_heartbeat(self, client_id, seq, addr):
    timestamp = time.strftime('%Y-%m-%d %H:%M:%S')
    print(f"[{timestamp}] Heartbeat received from client {client_id} at {addr}")
    client = self.clients[client_id]
    payload = pack_prompt_info(self.server_user, self.server_host, client.cwd)
    ack_msg = pack_msg(TYPE_ACK, seq, client_id, payload)
    self.sock.sendto(ack_msg, addr)
```

## 10.4 服务端心跳解析

服务端收到心跳后：

1. 打印日志。
2. 获取客户端状态。
3. 调用 `pack_prompt_info()` 封装提示符信息。
4. 调用 `pack_msg(TYPE_ACK, ...)` 封装 ACK。
5. 用 `sendto()` 发回客户端。

心跳 ACK 不只是确认在线，还顺便同步提示符信息。

## 10.5 离线清理完整代码

```python
def _cleanup_loop(self):
    while self.running:
        now = time.time()
        to_remove = []
        for cid, client in list(self.clients.items()):
            if now - client.last_heartbeat > 30:
                with client.process_lock:
                    proc = client.current_process
                if proc and proc.poll() is None:
                    self._kill_process_tree(proc)
                to_remove.append(cid)
        for cid in to_remove:
            print(f"Client {cid} timed out, removed")
            del self.clients[cid]

        time.sleep(5)
```

## 10.6 离线清理解析

```python
for cid, client in list(self.clients.items()):
```

使用 `list()` 拷贝一份条目，避免遍历字典时删除元素导致错误。

```python
if now - client.last_heartbeat > 30:
```

超过 30 秒没有报文，认为客户端离线。

```python
if proc and proc.poll() is None:
```

`poll()` 返回 None 表示进程还在运行。

如果客户端离线但进程还在运行，就杀掉进程树，避免孤儿进程占用资源。

---

# 11. Ctrl+C 中断与控制字符处理实现解析

## 11.1 客户端发送中断完整代码

```python
def _send_interrupt(self):
    try:
        msg = pack_msg(TYPE_INTERRUPT, self.send_seq, self.client_id, b'')
        self.sock.sendto(msg, self.server_addr)
    except Exception:
        pass
```

## 11.2 服务端处理中断完整代码

```python
def _handle_interrupt(self, client_id):
    client = self.clients.get(client_id)
    if not client:
        return

    with client.process_lock:
        proc = client.current_process

    if proc and proc.poll() is None:
        self._interrupt_process(proc)
```

```python
def _interrupt_process(self, proc):
    try:
        if os.name == 'nt':
            proc.send_signal(signal.CTRL_BREAK_EVENT)
            time.sleep(0.2)
            if proc.poll() is None:
                self._kill_process_tree(proc)
        else:
            os.killpg(os.getpgid(proc.pid), signal.SIGINT)
    except Exception:
        self._kill_process_tree(proc)
```

## 11.3 中断机制解析

客户端捕获 Ctrl+C 后，发送 `TYPE_INTERRUPT` 报文给服务端。

服务端根据 `client_id` 找到该客户端当前进程，然后调用 `_interrupt_process()`。

Windows 下：

1. 先发送 `CTRL_BREAK_EVENT`。
2. 等 0.2 秒。
3. 如果进程仍未退出，调用 `_kill_process_tree()` 强制结束。

Linux/macOS 下：

1. 使用 `os.killpg()` 给整个进程组发送 `SIGINT`。
2. 如果失败，强制杀掉进程树。

## 11.4 控制字符处理回顾

命令输入最终会经过：

```python
cmd = normalize_command_input(text).rstrip('\r\n')
```

它处理：

1. ANSI 转义序列。
2. Windows / Unix 换行差异。
3. 退格和 Delete。
4. 保留普通字符和 Tab。

---

# 12. 异常处理实现解析

## 12.1 非法报文

处理位置：`unpack_msg()`。

处理方式：返回 `None`。

调用方行为：

```python
msg = unpack_msg(data)
if not msg:
    continue
```

这样非法包不会影响主循环。

## 12.2 命令不存在

命令不存在时，shell 会把错误写到 stderr。

服务端通过：

```python
stderr=subprocess.PIPE
```

捕获错误输出，再通过 `_stream_pipe()` 发送给客户端。

## 12.3 服务端不可达

客户端启动时调用：

```python
connected = self._connect_to_server()
```

内部通过心跳等待 ACK。如果失败：

```python
Connection failed: server ... did not respond
```

## 12.4 网络中断

客户端心跳线程发现心跳 ACK 超时后调用：

```python
_handle_connection_error()
```

该函数会：

1. 设置 `running = False`。
2. 设置 `connected = False`。
3. 打印连接错误。
4. 关闭 socket。
5. 调用 `_thread.interrupt_main()` 打断主线程输入等待。

## 12.5 命令超时

命令执行线程中：

```python
proc.wait(timeout=30)
```

如果超时，杀掉进程树，并返回：

```text
Error: Command execution timed out
```

---

# 13. 测试验证逻辑解析

## 13.1 测试覆盖范围

`test_all.py` 覆盖了以下方面：

1. 协议封包解包。
2. 非法报文拒绝。
3. 序列号算法。
4. ANSI 过滤。
5. 输出结束标记。
6. 心跳 ACK。
7. 命令执行。
8. UTF-8 输出。
9. 实时输出。
10. 大输出滑动窗口传输。
11. 多客户端隔离。
12. 心跳超时。
13. 停等 ARQ 去重。
14. 控制字符处理。
15. 服务端不可达。
16. `cd` 持久化。

## 13.2 测试的意义

这些测试重点验证网络程序最容易出错的地方：

1. 丢包后是否重传。
2. 重复包是否会重复执行。
3. 输出乱序是否能恢复顺序。
4. 多客户端状态是否隔离。
5. 非法输入是否会导致崩溃。

---

# 14. 拔高功能实现解析

本节说明当前项目已经实现的三个拔高功能：动态滑动窗口流量控制、终端窗口大小同步、PTY/ConPTY 全屏交互式程序支持。

## 14.1 动态滑动窗口流量控制

### 14.1.1 功能目标

指导书要求发送端能够根据接收端缓冲区调整发送窗口，避免服务端输出过快导致客户端来不及缓存乱序分片。本项目在固定输出窗口基础上增加了接收端窗口通告，形成如下控制关系：

```text
实际发送窗口 = min(OUTPUT_WINDOW_SIZE, 客户端通告可接收分片数)
```

其中 `OUTPUT_WINDOW_SIZE` 是服务端本地允许的最大未确认输出分片数，客户端通告窗口表示客户端乱序缓冲区还能容纳多少分片。

### 14.1.2 协议层窗口通告格式

`common.py` 中定义了窗口通告前缀和封装函数：

```python
WINDOW_UPDATE_PREFIX = b'\x00WIN:'


def pack_window_update(available_packets: int, available_bytes: int) -> bytes:
    if not 0 <= available_packets <= 0xFFFF:
        raise ValueError("Available packet window out of range")
    if not 0 <= available_bytes <= 0xFFFFFFFF:
        raise ValueError("Available byte window out of range")
    return WINDOW_UPDATE_PREFIX + struct.pack('>H I', available_packets, available_bytes)


def unpack_window_update(data: bytes) -> tuple[int, int] | None:
    if not data.startswith(WINDOW_UPDATE_PREFIX):
        return None
    payload = data[len(WINDOW_UPDATE_PREFIX):]
    if len(payload) != struct.calcsize('>H I'):
        return None
    return struct.unpack('>H I', payload)
```

`available_packets` 使用 2 字节无符号整数，表示还能缓存多少个乱序分片；`available_bytes` 使用 4 字节无符号整数，表示还能缓存多少字节。前缀 `b'\x00WIN:'` 用于区分 ACK payload 中的窗口通告和普通空 ACK。

### 14.1.3 客户端接收窗口计算

客户端维护乱序缓存：

```python
self.recv_buffer = {}
self.recv_buffer_bytes = 0
self.recv_buffer_limit_packets = RECV_BUFFER_LIMIT_PACKETS
self.recv_buffer_limit_bytes = RECV_BUFFER_LIMIT_BYTES
```

计算剩余窗口的代码如下：

```python
def _available_recv_window_packets(self):
    return max(0, self.recv_buffer_limit_packets - len(self.recv_buffer))


def _available_recv_window_bytes(self):
    return max(0, self.recv_buffer_limit_bytes - self.recv_buffer_bytes)


def _make_window_update_payload(self):
    return pack_window_update(self._available_recv_window_packets(), self._available_recv_window_bytes())
```

当客户端收到 `TYPE_OUTPUT` 并成功接收分片时，ACK 会携带窗口通告：

```python
ack_msg = pack_msg(TYPE_ACK, seq, self.client_id, self._make_window_update_payload())
self.sock.sendto(ack_msg, self.server_addr)
```

如果乱序缓存已满，客户端不会错误 ACK 未保存的数据，而是发送独立的 `TYPE_WINDOW_UPDATE` 提醒服务端当前窗口状态。

### 14.1.4 服务端动态调整发送窗口

服务端在每个 `ClientInfo` 中保存客户端通告窗口：

```python
self.advertised_window_packets = OUTPUT_WINDOW_SIZE
self.advertised_window_bytes = RECV_BUFFER_LIMIT_BYTES
self.window_lock = threading.Lock()
```

ACK 或 `TYPE_WINDOW_UPDATE` 到达后，服务端解析并更新状态：

```python
def _apply_window_update(self, client_id, payload_or_update):
    if isinstance(payload_or_update, tuple):
        update = payload_or_update
    else:
        update = unpack_window_update(payload_or_update)
    if not update:
        return
    packets, bytes_available = update
    client = self.clients.get(client_id)
    if not client:
        return
    with client.window_lock:
        client.advertised_window_packets = max(0, min(OUTPUT_WINDOW_SIZE, packets))
        client.advertised_window_bytes = max(0, bytes_available)
```

发送时动态获取有效窗口：

```python
def _get_effective_send_window(self, client):
    with client.window_lock:
        return max(0, min(OUTPUT_WINDOW_SIZE, client.advertised_window_packets))
```

然后在滑动窗口循环中使用：

```python
effective_window = self._get_effective_send_window(client)
while next_to_send < len(packets) and next_to_send - base < effective_window:
    packet = packets[next_to_send]
    self._send_packet(client_id, addr, packet['seq'], packet['chunk'])
    packet['sent'] = True
    packet['last_sent'] = time.monotonic()
    next_to_send += 1
```

这使输出方向不再只是固定窗口，而是会受到客户端接收能力约束。

## 14.2 终端窗口大小同步 WINCH/Resize

### 14.2.1 功能目标

全屏程序需要知道终端行列数。例如 `vim` 要根据列数折行，`top` 要根据行数显示进程列表。项目通过 `TYPE_RESIZE` 报文同步客户端终端大小。

### 14.2.2 Resize 报文格式

`common.py` 中定义：

```python
RESIZE_PREFIX = b'\x00SZ:'


def pack_resize(rows: int, cols: int) -> bytes:
    if not 1 <= rows <= 1000:
        raise ValueError("Terminal rows out of range")
    if not 1 <= cols <= 1000:
        raise ValueError("Terminal columns out of range")
    return RESIZE_PREFIX + struct.pack('>H H', rows, cols)


def unpack_resize(data: bytes) -> tuple[int, int] | None:
    if not data.startswith(RESIZE_PREFIX):
        return None
    payload = data[len(RESIZE_PREFIX):]
    if len(payload) != struct.calcsize('>H H'):
        return None
    rows, cols = struct.unpack('>H H', payload)
    if not 1 <= rows <= 1000 or not 1 <= cols <= 1000:
        return None
    return rows, cols
```

行数和列数各使用 2 字节无符号整数，并限制在 1 到 1000，避免异常尺寸破坏服务端状态。

### 14.2.3 客户端发送窗口大小

客户端通过标准库获取终端大小：

```python
def _get_terminal_size(self):
    size = shutil.get_terminal_size(fallback=(80, 24))
    return size.lines, size.columns
```

发送 resize：

```python
def _send_resize(self, rows=None, cols=None):
    try:
        if rows is None or cols is None:
            rows, cols = self._get_terminal_size()
        rows = max(1, min(1000, int(rows)))
        cols = max(1, min(1000, int(cols)))
        self.last_rows = rows
        self.last_cols = cols
        msg = pack_msg(TYPE_RESIZE, self.resize_seq, self.client_id, pack_resize(rows, cols))
        self.sock.sendto(msg, self.server_addr)
        self.resize_seq = next_data_seq(self.resize_seq)
    except Exception:
        pass
```

客户端在三种时机发送窗口大小：连接成功后、进入 PTY 命令前、交互循环中检测窗口变化时。

POSIX 平台还会安装 `SIGWINCH` 处理器：

```python
def _install_resize_handler(self):
    if os.name == 'nt' or not hasattr(signal, 'SIGWINCH'):
        return
    try:
        self.saved_sigwinch_handler = signal.getsignal(signal.SIGWINCH)

        def handle_winch(signum, frame):
            self._send_resize()

        signal.signal(signal.SIGWINCH, handle_winch)
    except (ValueError, AttributeError):
        pass
```

Windows 没有 POSIX `SIGWINCH`，因此在交互循环中使用轮询方式检测变化。

### 14.2.4 服务端应用窗口大小

服务端收到 `TYPE_RESIZE` 后更新客户端状态并立即尝试调整 PTY：

```python
def _handle_resize(self, client_id, seq, payload, addr):
    client = self.clients.get(client_id)
    if not client:
        return
    size = unpack_resize(payload)
    if not size:
        return
    rows, cols = size
    client.term_rows = rows
    client.term_cols = cols
    self._resize_client_pty(client, rows, cols)
    ack_msg = pack_msg(TYPE_ACK, seq, client_id, b'')
    self.sock.sendto(ack_msg, addr)
```

POSIX PTY 使用 `ioctl(TIOCSWINSZ)`：

```python
def _set_pty_size_fd(self, fd, rows, cols):
    if os.name == 'nt' or fd is None:
        return
    try:
        fcntl.ioctl(fd, termios.TIOCSWINSZ, winsize_struct.pack('HHHH', rows, cols, 0, 0))
    except Exception:
        pass
```

Windows ConPTY 通过 `pywinpty` 暴露的 resize 方法设置大小。

## 14.3 PTY/ConPTY 全屏交互式程序支持

### 14.3.1 命令识别策略

项目保留普通命令路径，同时为交互式全屏程序启用 PTY 路径。`common.py` 中定义：

```python
PTY_PREFIX = 'pty'
PTY_COMMANDS = {'top', 'vim', 'vi', 'nano', 'less'}
```

判断函数：

```python
def should_use_pty_command(cmd: str) -> bool:
    tokens = _split_command(cmd)
    if not tokens:
        return False
    first = tokens[0].lower()
    if first == PTY_PREFIX:
        return len(tokens) > 1
    exe = os.path.basename(first).lower()
    if exe.endswith('.exe'):
        exe = exe[:-4]
    return exe in PTY_COMMANDS
```

这支持两种进入 PTY 的方式：显式输入 `pty cmd`、`pty vim README.md`，或直接输入 `vim README.md`、`top` 等已知全屏程序。

### 14.3.2 客户端 raw 输入与 STDIN 转发

进入 PTY 命令前，客户端设置交互模式并发送当前窗口大小。POSIX 平台使用 `tty.setraw()` 让本地终端进入 raw mode：

```python
def _enter_raw_mode(self):
    if os.name == 'nt' or not sys.stdin.isatty() or self.raw_terminal_attrs is not None:
        return
    try:
        fd = sys.stdin.fileno()
        self.raw_terminal_attrs = termios.tcgetattr(fd)
        tty.setraw(fd)
    except Exception:
        self.raw_terminal_attrs = None
```

交互循环读取按键并发送 `TYPE_STDIN`：

```python
def _send_stdin(self, data):
    for i in range(0, len(data), MAX_DATA_SIZE):
        chunk = data[i:i + MAX_DATA_SIZE]
        try:
            msg = pack_msg(TYPE_STDIN, self.stdin_seq, self.client_id, chunk)
            self.sock.sendto(msg, self.server_addr)
            self.stdin_seq = next_data_seq(self.stdin_seq)
        except Exception:
            return False
    return True
```

Windows 平台使用 `msvcrt.getwch()` 读取按键，并把方向键、Home、End、Delete 等转换成常见 ANSI 按键序列；中文字符和代理对字符转换成 UTF-8 字节发送。

### 14.3.3 服务端 STDIN 处理

服务端收到 `TYPE_STDIN` 后先 ACK，再按序列号更新输入顺序，并把数据放入该客户端的待写入队列：

```python
def _handle_stdin(self, client_id, seq, payload, addr):
    client = self.clients.get(client_id)
    if not client:
        return

    ack_msg = pack_msg(TYPE_ACK, seq, client_id, b'')
    self.sock.sendto(ack_msg, addr)

    expected = client.stdin_recv_expected_seq
    if seq == expected:
        client.stdin_recv_expected_seq = next_data_seq(expected)
    elif is_sequence_ahead(seq, expected):
        client.stdin_recv_expected_seq = next_data_seq(seq)
    else:
        return

    with client.pty_lock:
        if client.is_interactive and len(client.pending_stdin) < 128:
            client.pending_stdin.append(payload)
            client.stdin_event.set()
```

执行线程周期性调用 `_flush_pending_stdin()`，把积累的按键写入 PTY/ConPTY。

### 14.3.4 POSIX PTY 实现

Linux/macOS/WSL 下使用 Python 标准库 `pty.openpty()` 创建伪终端：

```python
master_fd, slave_fd = pty.openpty()
self._set_pty_size_fd(slave_fd, client.term_rows, client.term_cols)
proc = subprocess.Popen(
    ['bash', '-lc', cmd],
    stdin=slave_fd,
    stdout=slave_fd,
    stderr=slave_fd,
    cwd=client.cwd,
    preexec_fn=os.setsid,
    close_fds=True
)
```

服务端保存 `master_fd`，后续把客户端按键写入 master fd，并从 master fd 读取全屏程序输出。PTY 输出不会经过 ANSI 过滤，而是原样返回客户端。

### 14.3.5 Windows ConPTY 实现

Windows 平台使用可选依赖 `pywinpty`：

```python
class WindowsPtySession:
    def __init__(self, cmd, cwd, rows, cols):
        try:
            PtyProcess = importlib.import_module('winpty').PtyProcess
        except ImportError as exc:
            raise RuntimeError("Windows PTY requires pywinpty; install it with: pip install pywinpty") from exc

        self.proc = PtyProcess.spawn(cmd, cwd=cwd, dimensions=(rows, cols))
        self.stdin_decoder = codecs.getincrementaldecoder('utf-8')(errors='ignore')
        self.resize(rows, cols)
```

实际代码为了兼容不同版本 `pywinpty`，会尝试多种参数组合，例如只传 `cwd`、只传 `dimensions` 或不传可选参数。

写入时，服务端把客户端 UTF-8 字节增量解码成字符串，再写入 ConPTY：

```python
def write(self, data):
    text = self.stdin_decoder.decode(data)
    if text:
        self.proc.write(text)
```

读取时，服务端从 ConPTY 读取输出并编码为 UTF-8 字节，再走可靠输出链路返回客户端。

### 14.3.6 PTY 输出渲染策略

普通命令输出在客户端显示前会过滤 ANSI：

```python
sys.stdout.write(strip_ansi_sequences(payload).decode('utf-8', errors='replace'))
```

PTY 交互模式下不能过滤 ANSI，而是直接写入 stdout buffer：

```python
out = getattr(sys.stdout, 'buffer', None)
if out is not None:
    out.write(payload)
    out.flush()
```

这是支持 `vim`、`top`、`less` 这类全屏程序的关键。清屏、光标移动、颜色、备用屏幕都依赖 ANSI 控制序列，如果过滤掉，界面会失效。

### 14.3.7 会话清理与多客户端隔离

每个客户端都有独立状态：

```python
self.pty_fd = None
self.pty_session = None
self.pty_lock = threading.Lock()
self.is_interactive = False
self.stdin_recv_expected_seq = 0
self.pending_stdin = []
self.term_rows = 24
self.term_cols = 80
```

客户端超时、服务端关闭或 PTY 命令结束时，服务端会清理 PTY/ConPTY、当前进程和待输入队列，避免资源泄漏，也避免不同客户端互相影响。

---

# 15. 模块交互关系与完整调用流程

## 15.1 客户端启动流程

```text
main
 ↓
UDPClient(args.host, args.port)
 ↓
client.start()
 ↓
启动 _recv_loop 接收线程
 ↓
_connect_to_server()
 ↓
_wait_for_heartbeat_ack()
 ↓
_send_heartbeat()
 ↓
收到服务端 TYPE_ACK
 ↓
启动 _heartbeat_loop 心跳线程
 ↓
进入 input() 命令循环
```

## 15.2 服务端启动流程

```text
main
 ↓
UDPServer(args.host, args.port)
 ↓
server.start()
 ↓
启动 _recv_loop 接收线程
 ↓
启动 _cleanup_loop 清理线程
 ↓
主线程 sleep 等待 Ctrl+C
```

## 15.3 一条普通命令的完整调用链

```text
客户端 input()
 ↓
_send_command_reliable()
 ↓
_send_reliable()
 ↓
pack_msg(TYPE_COMMAND)
 ↓
socket.sendto()
 ↓
服务端 _recv_loop()
 ↓
unpack_msg()
 ↓
_handle_command()
 ↓
pack_msg(TYPE_ACK)
 ↓
socket.sendto(ACK)
 ↓
服务端启动 _execute_and_respond() 线程
 ↓
subprocess.Popen()
 ↓
_stream_pipe(stdout/stderr)
 ↓
_send_output_reliable()
 ↓
_send_output_chunks_reliable()
 ↓
pack_msg(TYPE_OUTPUT)
 ↓
客户端 _recv_loop()
 ↓
pack_msg(TYPE_ACK)
 ↓
_handle_output_packet()
 ↓
_process_output_payload()
 ↓
sys.stdout.write()
```

## 15.4 心跳调用链

```text
客户端 _heartbeat_loop()
 ↓
_wait_for_heartbeat_ack()
 ↓
_send_heartbeat()
 ↓
pack_msg(TYPE_HEARTBEAT)
 ↓
服务端 _recv_loop()
 ↓
_handle_heartbeat()
 ↓
pack_prompt_info()
 ↓
pack_msg(TYPE_ACK)
 ↓
客户端 _recv_loop()
 ↓
unpack_prompt_info()
 ↓
_apply_prompt_info()
 ↓
heartbeat_ack_event.set()
```

---

# 16. 性能优化、边界情况与设计取舍

## 16.1 性能优化措施

### 16.1.1 控制单包大小

`MAX_DATA_SIZE = 1472 - HEADER_SIZE` 尽量避免 IP 分片，提高 UDP 报文成功率。

### 16.1.2 输出方向滑动窗口

固定窗口大小 8，减少停等等待时间，提高大输出传输效率。

### 16.1.3 线程分工

客户端：

1. 主线程负责用户输入。
2. 接收线程负责收 ACK 和输出。
3. 心跳线程负责保活。

服务端：

1. 接收线程负责收 UDP 报文。
2. 清理线程负责离线客户端。
3. 每条命令独立线程执行。
4. stdout/stderr 各独立线程读取。

这种设计避免单个慢命令阻塞整个服务端。

### 16.1.4 增量解码

使用增量解码器避免多字节字符跨 chunk 时解码失败，提高中文输出稳定性。

## 16.2 边界情况处理

| 边界情况 | 处理方式 |
|---|---|
| 非法魔数 | `unpack_msg()` 返回 `None` |
| 非法类型 | `unpack_msg()` 返回 `None` |
| 数据长度不匹配 | `unpack_msg()` 返回 `None` |
| 命令 ACK 丢失 | 客户端超时重传，服务端去重 |
| 输出分片丢失 | 服务端超时重传 |
| 输出分片乱序 | 客户端 `recv_buffer` 缓存 |
| 客户端离线 | 服务端 30 秒清理 |
| 服务端不可达 | 客户端心跳连接失败 |
| 命令超时 | 服务端杀进程树 |
| Ctrl+C | 客户端发送中断报文，服务端中断进程 |
| 中文输出跨字节 | 增量解码处理 |

## 16.3 设计取舍

### 16.3.1 为什么命令方向用停等 ARQ

命令输入通常很短，而且用户一次只输入一条命令。停等 ARQ 简单可靠，足够满足需求。

### 16.3.2 为什么输出方向用滑动窗口

输出可能很大，如果仍然停等，会严重影响速度。因此输出方向使用窗口传输。

### 16.3.3 为什么不实现完整 TCP

实验目标是基于 UDP 自定义可靠机制，而不是重新实现完整 TCP。

当前项目实现了必要的：

1. ACK。
2. 重传。
3. 序列号。
4. 去重。
5. 有序显示。
6. 心跳保活。

但没有实现完整 TCP 的拥塞控制、三次握手、四次挥手等。

### 16.3.4 为什么普通命令和 PTY 命令分成两条路径

当前项目没有把所有命令都放入 PTY，而是保留普通 PIPE 路径，并只在 `pty <command>` 或 `top`、`vim`、`less` 等全屏命令时进入 PTY/ConPTY 路径。

这样设计的原因是：

1. 普通命令使用 PIPE 更容易做文本解码、错误捕获和超时控制。
2. 普通命令输出可以过滤 ANSI，显示更干净。
3. PTY 命令需要 raw 输入、逐字节转发和 ANSI 原样输出，复杂度更高。
4. Windows ConPTY 依赖 `pywinpty`，将其作为交互式命令路径的可选能力更稳妥。
5. 两条路径分离后，PTY 出问题不会影响普通命令执行。

因此项目采用“普通命令走 PIPE，全屏交互命令走 PTY/ConPTY”的分层设计。

---

# 17. 总结

当前项目的核心技术路线是：

```text
UDP socket
    + 自定义应用层协议
    + 魔数校验
    + 类型字段
    + 客户端 ID
    + 序列号
    + ACK
    + 超时重传
    + 命令方向停等 ARQ
    + 输出方向滑动窗口可靠传输
    + 接收端窗口通告
    + 服务端动态发送窗口调整
    + 客户端乱序缓存
    + 心跳保活
    + 多客户端状态隔离
    + subprocess 普通命令执行
    + stdout/stderr 实时读取
    + Ctrl+C 中断远程进程
    + Resize/WINCH 终端窗口同步
    + PTY/ConPTY 全屏交互程序支持
    + raw 输入和 TYPE_STDIN 按键转发
```

从功能完成情况看：

| 类型 | 完成情况 |
|---|---|
| 基础功能 | 已完成并通过测试 |
| 进阶功能 | 已完成并通过测试 |
| 拔高功能 | 已完成并通过测试 |

从工程实现角度看，项目已经把 UDP 的不可靠数据报能力扩展成了一个具备可靠性、顺序性、多客户端隔离、实时输出、动态流量控制、终端窗口同步和 PTY/ConPTY 全屏交互能力的远程终端系统。

本次重新验证后，自动化测试覆盖了协议、命令执行、实时输出、多客户端、心跳、ARQ、控制字符、动态窗口通告、Resize 状态更新和 PTY 简化交互。安装 `pywinpty` 后，Windows 环境下 `pty cmd` 简化交互链路也已通过独立验证。
