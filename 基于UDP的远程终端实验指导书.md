# 基于UDP的远程终端程序设计与实现指导书

## 一、实验目的

1. 掌握 **UDP 无连接、不可靠、面向报文** 的特点，熟练编写 UDP 服务端 / 客户端程序。
2. 设计并实现 **自定义应用层协议**，解决报文边界、类型区分、乱序、重复等问题。
3. 在应用层实现 **停等 ARQ 可靠传输**，使不可靠的 UDP 支持稳定的终端交互。
4. 理解远程终端的基本原理，处理 **回车、退格、Ctrl+C** 等控制字符。
5. 实现 **多客户端并发**、**心跳保活**，提升系统健壮性。
6. 培养协议设计、异常处理、实验分析与报告撰写能力。

## 二、实验原理

2.1 UDP 协议特点

- 无连接，不需要三次握手
- 不保证可靠，报文可能丢失、乱序、重复
- 面向报文：一次 sendto 对应一次 recvfrom，**不存在 TCP 粘包**
- 必须在应用层自行解决可靠性问题

2.2 应用层可靠传输（停等 ARQ）

- 发送报文 → 等待 ACK
- 超时未收到 ACK → 重传
- 使用 **序列号** 区分新旧报文、去重、保证有序

2.3 远程终端基本原理

- 客户端把键盘输入（包括控制字符）通过 UDP 发给服务端
- 服务端创建子进程执行命令，并将输出返回客户端
- 客户端在本地终端显示执行结果
- 需处理：回车、换行、退格、Ctrl+C 等终端控制字符

2.4、自定义应用层协议

供参考。报文固定头部（必做）

<table><tr><td><p><strong>字段</strong></p></td><td><p><strong>长度（字节）</strong></p></td><td><p><strong>说明</strong></p></td></tr><tr><td><p>魔数</p></td><td><p>2</p></td><td><p>固定 0x5554，用于识别合法报文</p></td></tr><tr><td><p>报文类型</p></td><td><p>1</p></td><td><p>0x01 命令输入</p><p>0x02 命令输出</p><p>0x03 ACK</p><p>0x04 心跳</p></td></tr><tr><td><p>序列号</p></td><td><p>4</p></td><td><p>用于去重、有序、重传判断</p></td></tr><tr><td><p>数据长度</p></td><td><p>2</p></td><td><p>数据段长度</p></td></tr><tr><td><p>客户端 ID</p></td><td><p>4</p></td><td><p>用于区分多客户端</p></td></tr><tr><td><p>数据段</p></td><td><p>可变</p></td><td><p>命令 / 输出内容</p></td></tr></table>

## 三、实验内容

### 3.1 基础功能

所有学生必须完成以下全部内容：

1. **UDP 基础通信**
   - 客户端可指定服务端 IP 和端口
   - 服务端绑定端口并接收报文
2. **自定义协议封包 / 解包**
   - 正确解析魔数、类型、序列号、客户端 ID、数据长度
   - 非法报文直接丢弃
3. **停等 ARQ 可靠传输**
   - 超时重传
   - 序列号去重
   - ACK 确认机制
4. **基础终端命令执行**
   - 支持 ls、pwd、ps、ip addr 等非交互式命令
   - 正确显示输出
   - 处理 \n、\r、\b 等基础控制字符
5. **多客户端支持**
   - 服务端根据 **客户端 ID** 区分不同用户
   - 多客户端同时连接不混乱、不互相干扰
6. **心跳检测**
   - 客户端每 5 秒发送心跳
   - 服务端收到心跳回复 ACK
   - 长时间无心跳视为离线
7. **基本异常处理**
   - 命令不存在
   - 服务端不可达
   - 网络中断提示
   - 程序不崩溃

### 3.2 进阶功能

1. **终端控制字符增强**
   - 支持 **Ctrl+C** 中断服务端正在运行的命令
   - 正确处理退格、换行、Tab
   - 过滤 ANSI 转义序列，避免乱码
2. **回退 N 步 ARQ**
   - 滑动窗口发送
   - 提高大批量输出的传输效率
3. **实时命令输出**
   - 支持 ping 这类持续输出命令
   - 实时回显，不等待命令结束

### 3.3 拔高功能

1. **滑动窗口流量控制**
   - 根据接收端缓冲区调整发送窗口
   - 防止发送过快导致丢包
2. **终端窗口大小同步（WINCH）**
   - 客户端将终端行列数发给服务端
   - 服务端设置伪终端大小
3. **支持 top /vim 类全屏交互式程序**
   - 依赖伪终端 PTY
   - 支持光标移动、界面刷新、全屏渲染
   - 可正常操作与退出

## 四、实验环境与分组

分组：每2人一组，配合完成。

实验环境：Windows/Linux/macOS均可，需具备以下条件：

1）安装基础网络调试工具（用于协议抓包、数据分析，如Wireshark）；

2）服务器程序运行在云服务器中；

## 五、实验步骤

1. **学习 UDP 编程**

实现简单的客户端 / 服务端消息收发。

1. **实现自定义协议**

完成封包、解包、魔数校验、序列号管理。

1. **实现停等 ARQ**

超时、重传、ACK、去重。

1. **实现命令执行与终端输出**

调用系统命令、捕获 stdout/stderr。

1. **加入多客户端与心跳**

完成基础全部要求。

1. **扩展进阶 / 拔高功能**

如实时输出、Ctrl+C、PTY、top/vim 支持。

1. **测试与抓包验证**

使用 Wireshark 观察 UDP 报文与协议格式。

1. **撰写实验报告**

测试：

1. 单个客户端执行 ls 正常输出
2. 多客户端同时操作不混乱
3. 断开网络后出现超时重传
4. 心跳正常，离线可检测
5. ping 命令可实时输出并能用 Ctrl+C 中断
6. 拔高：top 可正常显示界面并退出

## 六、注意事项

1. UDP 报文建议不超过 1472 字节，避免分片。
2. 服务端必须用客户端 ID 区分用户。
3. 终端控制字符要透明传输，不能过滤。
4. 使用 PTY 才能支持 top/vim。
5. 程序退出时需恢复终端模式。
